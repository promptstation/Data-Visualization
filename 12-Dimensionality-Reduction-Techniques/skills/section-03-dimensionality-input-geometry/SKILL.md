---
name: section-03-dimensionality-input-geometry
description: Develop comprehensive, professional-level learning modules and training materials on input Geometry — Distances, Similarities, and Preprocessing for Embeddings within Dimensionality Reduction Techniques for Visualization — engineer embedding inputs through preprocessing pipelines (cleaning-first doctrine with missing-outlier strategies, scaling-normalization systems with method interactions, transformation decisions for skew and composition, feature-space construction awareness); build distance-similarity inputs (metric-selection.... Use this skill whenever the user asks to create, teach, or deepen training on input, geometry, distances, similarities, preprocessing, embeddings, Dimensionality reduction, or requests workshops, lessons, curricula, job aids, or expert instruction in this area — even when the word “module” is never used.
compatibility: Any environment that can write Markdown files; benefits from web research access for authoritative sources; no external tools or packages required.
metadata:
  author: Promptstation skill-building pipeline (Data Visualization course 12, section 3)
  version: 1.0.0
  category: professional-education
---

# Input Geometry: Distances, Similarities, and Preprocessing for Embeddings — Professional Training

## Mission

Produce professional-grade learning modules and training materials on **input Geometry: Distances, Similarities, and Preprocessing for Embeddings** within The embedding science of the data visualization specialist discipline — PCA through t-SNE and UMAP mastered as visualization instruments: the mathematics of neighbor preservation, the parameter craft, the artifact literacy, and the evaluation discipline that separate trustworthy embeddings from beautiful fictions.

Subject scope: Covers the input layer whose quality determines every embedding's worth: the preprocessing pipeline — the embedding precondition: the cleaning-first doctrine (the section-2-of-the-EDA-discipline application: the missing-values-duplicates-and-errors resolution before any reduction, the missing-data strategies for embeddings: the imputation-versus-complete-case decisions with the imputation-artifact-in-neighbor-structure caution, the outlier-handling decisions: the extreme-values-distorting-neighbor-geometry problems with the transform-cap-or-robust-method options); the scaling-and-normalization systems (the standardization default reasoning: the equal-feature-contribution logic for variance-based methods, the scale-choice-method-interaction: the PCA-covariance-versus-correlation and neighbor-graph-distance sensitivities to scaling from the metric traditions, the per-feature-versus-per-sample normalization: the row-and-column-normalization decisions by data semantics — the gene-expression-and-document-frequency conventions from the domain traditions, the range-and-robust-scaling alternatives: the min-max-and-quantile-based options for outlier-heavy data from the resistant-statistics traditions); the transformation decisions (the log-and-power transforms for skewed features: the multiplicative-structure-linearization from the EDA transformation systems, the compositional-data handling: the CLR-and-log-ratio transforms for constant-sum data from the compositional traditions, the binarization-and-coding choices for categorical features: the one-hot-versus-embedding-input decisions with the high-cardinality-dilution caution); and the feature-space construction awareness (the raw-features-versus-derived-representations decision: the embedding-pretrained-features for images-text-and-audio from the representation-learning traditions, the feature-selection pre-step: the removing-uninformative-and-redundant-variables improving neighbor structure, the curse-mitigation preprocessing: the variance-threshold-and-correlation-pruning reductions before the main reduction) as the pipeline foundation; the distance-and-similarity input layer — what neighbors mean: the metric-selection discipline (the Euclidean-default examination: the when-standard-distance-fits diagnostic from the metric traditions of the geometry course, the domain-appropriate-measures: the Bray-Curtis-for-ecological-counts, Jaccard-for-binary, cosine-for-text-and-embeddings, and correlation-distance conventions from the domain literatures, the metric-method-compatibility: the which-DR-methods-accept-custom-distances capability matrix — the MDS-flexibility versus t-SNE-UMAP-affinity-inputs distinctions); the similarity-and-affinity construction (the affinity-matrix concept: the pairwise-similarity inputs for stochastic-neighbor methods previewing section 5's mechanics, the kernel-bandwidth decisions: the Gaussian-kernel-width selection affecting local-neighborhood scales, the kNN-graph construction: the k-selection and symmetric-versus-mutual-kNN variants as the neighbor-structure foundation for section 4's methods and UMAP); the high-dimensional-distance pathologies (the concentration-effect consequences for neighbor graphs: the noisy-neighborhood-identification in raw high dimensions, the hubness-correction awareness: the hub-node-artifacts-in-kNN-graphs and the mutual-kNN-and-normalization mitigations from the neighbor-search literature, the intrinsic-dimension estimation as preprocessing diagnostic: the expecting-structure-worth-unrolling verification); the distance-computation-at-scale (the pairwise-matrix O(n²) memory reality: the large-n strategies from the scale traditions, the approximate-nearest-neighbor libraries: the Annoy-HNSW-pynndescent-family acceleration underlying modern UMAP implementations, the metric-computation-precision: the float32-versus-float64 distance effects from the numerical traditions); and the input-documentation obligation (the preprocessing-decision-log: the every-transform-scale-and-filter recorded for reproducibility from the EDA documentation systems, the distance-choice-rationale: the metric-selection-justification for method comparisons) as the input layer; the neighbor-graph foundations — the local-structure machinery: the kNN-graph anatomy (the directed-versus-symmetric-graph conventions: the mutual-neighborhood-strictness spectrum, the k-parameter semantics: the local-scale-control reading shared across Isomap-LLE-UMAP families, the graph-connectivity diagnostics: the component-fragmentation detection and repair strategies); the graph-weighting schemes (the binary-adjacency versus distance-weighted edges: the weight-construction choices affecting downstream layouts, the heat-kernel-and-t-SNE-affinity weighting: the probability-style weights previewing the stochastic methods, the UMAP-fuzzy-membership weights: the local-connectivity-normalized construction previewing section 7's mechanics); the geodesic-distance concept (the graph-shortest-path-distances as manifold-geodesic-approximations: the Isomap core insight from the manifold-learning tradition, the path-versus-ambient-distance distinction: the along-the-manifold versus through-space measurement difference, the shortest-path-computation realities: the Dijkstra-and-Floyd-Warshall costs and the landmark approximations from the scale traditions); the local-linearity assumptions (the locally-linear-manifold hypothesis: the neighborhood-flatness premise underlying LLE-family methods, the neighborhood-size-versus-curvature tension: the too-large-k-violating-linearity tradeoff, the assumption-violation diagnostics: the reconstruction-error patterns revealing non-manifold structure); and the neighbor-quality-assessment (the neighborhood-stability-under-perturbation checks: the noise-robustness of identified neighbors, the k-sensitivity-exploration discipline: the multiple-k-comparison protocol, the visual-neighbor-verification: the known-category-cohesion-in-neighborhoods spot checks) as the graph layer; the data-type adaptation — the input variety: the numeric-matrix defaults (the continuous-feature standard paths through all methods, the mixed-scale handling from the normalization systems above); the categorical-and-mixed-data strategies (the Gower-distance-and-similarity conventions for mixed types at practitioner awareness from the clustering traditions, the encoding-then-embedding versus native-distance decisions: the one-hot-dilution versus custom-metric tradeoffs, the ordinal-data handling: the rank-and-interval-encoding choices preserving order semantics); the sequence-and-graph-structured inputs (the node-embedding contexts: the graph-structure-to-vector representations from the network-embedding traditions at awareness, the trajectory-and-sequence distances: the DTW-and-edit-distance inputs to MDS-family methods at awareness, the precomputed-distance-matrix workflows: the distance-input-only methods for non-vectorizable data); the image-text-and-embedding inputs (the pretrained-representation projection: the embedding-space visualization as the dominant modern application from the AI traditions, the feature-extraction-pipeline awareness: the CNN-and-transformer-representations as DR inputs, the high-dimensional-embedding-specific pathologies: the anisotropy-and-cone-effects in text embeddings at awareness from the NLP literature); and the sparse-data handling (the sparse-matrix-support realities: the method-and-library capabilities for document-and-count matrices, the sparsity-specific-distance considerations: the cosine-and-Jaccard advantages in sparse regimes, the zero-inflation awareness: the dropout-and-count-data effects on neighbor structure) as the adaptation layer; the preprocessing-experiment discipline — the input-sensitivity practice: the preprocessing-variant-comparison protocol (the parallel-pipelines-with-variation documentation: the scaled-versus-unscaled-transformed-versus-raw embedding comparisons, the structure-stability-across-preprocessing assessment: the findings-robustness-to-input-choices verification from section 11's evaluation systems); the ablation-style-input-testing (the feature-subset-embeddings: the which-variables-drive-structure identification, the transform-effect-isolation: the single-preprocessing-change-at-a-time comparisons); the domain-informed-validation (the known-structure-recovery tests: the synthetic-and-semi-synthetic benchmarks with planted clusters and manifolds, the biological-or-domain-marker coherence checks: the expected-group-separation verification in applied contexts from the single-cell traditions); the preprocessing-reporting-standards (the methods-section completeness: the publication-grade preprocessing documentation conventions, the reproducibility-package inclusion: the pipeline-code-and-parameters with data versions from the EDA documentation discipline); and the garbage-in-recognition (the preprocessing-failure-symptom map: the hairball-embeddings-from-unscaled-data and artifact-clusters-from-batch-effects diagnostics, the iterate-preprocessing-and-embedding loops: the input-refinement-driven-by-output-inspection workflow) as the experiment layer; and the section anti-patterns — the failure library: the raw-data-embedding skipping all preprocessing, remedied by the cleaning-scaling-transform pipeline; the scale-blindness letting high-variance features dominate neighbor structure, remedied by the normalization-reasoning discipline; the metric-autopilot applying Euclidean distance to compositional or text data, remedied by the domain-appropriate-measure selection; the k-arbitrariness fixing neighbor counts without sensitivity exploration, remedied by the multi-k comparison protocol; the hubness-unawareness letting hub nodes distort graph structure, remedied by the mutual-kNN and correction awareness; the imputation-artifact-blindness reading imputed-value neighborhoods as real structure, remedied by the imputation-caution and complete-case comparisons; the precomputed-distance-opacity using similarity matrices without documenting their construction, remedied by the input-documentation obligations; and the single-preprocessing-trust reporting one pipeline's embedding as the dataset's structure, remedied by the variant-comparison and stability discipline with detection methods as the diagnostic.

Write as an experienced practitioner, not as a summarizer of popular content. Every framework taught must be something a real team or professional could run: procedures they can execute, criteria they can judge with, and artifacts they can hand to a colleague. Do not present claims as settled when the field treats them as contested — the training must model evidence discipline.

The module deepens this section's capabilities for a learner progressing from competent beginner toward expert practitioner, and connects them to the surrounding discipline rather than teaching them in isolation.

## Use Cases

### Full learning module
When asked for a comprehensive module on this topic:
1. Scope audience, prerequisites, duration, and discipline mix.
2. Build the evidence base from the authoritative sources below.
3. Write the full progressive module from the template.
4. Include all exercises with model solutions and all gate checklists.
5. Validate against the gate below before delivery.

### Condensed workshop
When asked for a one-day or half-day workshop:
1. Prioritize the units that match where the group is stuck.
2. Compress content to frameworks plus one worked example each; run exercises live with the participants' own material.
3. Leave behind the relevant checklists as job aids.

### Working job aids
When a practitioner needs tools rather than teaching:
1. Deliver the applicable checklists and templates from `references/exercise-and-checklist-library.md`, customized to their situation.
2. Add a one-page rationale per aid so the user understands what each item protects against.

## Core Output Requirements

- Deliverables are Markdown documents: the module, exercise sets with model solutions, and checklists. No placeholders, no "TODO" sections.
- Ground content in authoritative sources:
- The dimensionality-reduction canon (the PCA-MDS-Isomap-LLE-spectral-embedding lineage from the manifold-learning literature)
- The t-SNE primary literature (the van der Maaten-Hinton stochastic-neighbor-embedding tradition with the Barnes-Hut and FIt-SNE scaling extensions)
- The UMAP primary literature (the McInnes-Healy-Melville uniform-manifold-approximation tradition with its fuzzy-simplicial-set foundations)
- The neighbor-graph and manifold assumption foundations (the kNN-graph, local-linearity, and geodesic-distance traditions underlying modern methods)
- The embedding-evaluation literature (the trustworthiness-continuity, procrustes-alignment, and stability-assessment traditions from the visualization research canon)
- The visualization-research critique tradition (the how-to-use-t-SNE-effectively guidance, the artifact-and-misinterpretation studies from the EGD-lineage literature)
- The implementation ecosystem documentation (the scikit-learn, umap-learn, openTSNE, and GPU-accelerated library materials for practical computation)
- Maintain an evidence ledger while writing: every factual claim or number is either sourced, flagged as disputed/popular account, or omitted. Never invent statistics, studies, or citations.
- Distinguish established research findings from professional conventions and informed recommendations, and say which is which.
- Explain each specialized term in clear language on first use.

## Module Development Workflow

### Phase 1 — Scope and audience
Determine delivery mode (full module / workshop / job aids), learner background, duration, and whether learners bring their own material to work on. Record these choices; they drive depth allocation in Phase 3.

### Phase 2 — Evidence base
Collect the strongest documented examples, findings, and case material for this topic from the authoritative sources. Note what is well established, what is contested, and what is merely conventional. Verify any numbers before publishing them.

### Phase 3 — Architecture
Sequence the material progressively and establish core conceptual distinctions before the concepts are used together. Suggested unit sequence:

1. Unit 1

### Phase 4 — Write the units
For each unit follow the internal structure: teach the framework → show a worked example (weak / improved / professional versions where useful) → connect back to the surrounding discipline → state trade-offs explicitly. Use `references/domain-content-map.md` as the unit-by-unit source of scope, bullets, and evidence guidance.

### Phase 5 — Exercises and assessment
Select and adapt exercises from `references/exercise-and-checklist-library.md`. Adapt scenarios to the audience's domain. For a full module, include expert-quality model solutions; for workshops, convert selected exercises into facilitated live activities.

### Phase 6 — Checklists and job aids
Include the gate checklists from the library, customized to the audience's context without diluting the decision each item forces.

### Phase 7 — Validation gate
Run the Validation Gate below against the finished material before delivery. Fix failures; do not ship and caveat.

## Module Template

ALWAYS use this exact template for full modules:

```markdown
# Input Geometry: Distances, Similarities, and Preprocessing for Embeddings [— audience/context subtitle]

## Who This Module Is For
## Prerequisites
## Learning Outcomes
## Unit 1 — Unit 1
## Integrated Capstone
## Practical Exercises
## Professional Checklists
## Sources and Evidence Notes
```

Each unit internally follows: framework → worked example(s) → disciplinary connection → trade-offs.

## Writing Standards

Throughout the material, prioritize language that is:

* Precise without becoming jargon-heavy
* Practical without discarding rigor
* Honest about limitations and contested findings without being defeatist
* Concrete — anchored in real cases, real artifacts, and verifiable numbers
* Progressive from fundamentals to expert judgment

Where a recommendation depends on context, explain the trade-off rather than presenting an absolute rule.

## Validation Gate

Before delivery, verify:

### Content
- all units present with correct depth for the scoped audience
- core distinctions established before they are used together
- every taught capability has a usable framework, not just an explanation

### Evidence
- every claim and number is sourced or explicitly flagged as disputed
- no invented statistics, studies, dates, or citations anywhere
- sources are authoritative; no SEO-farm or marketing claims presented as fact

### Capability
- each absorbed capability (4 in this section) is covered by teaching content AND at least one exercise with evaluation criteria
- exercises have model solutions in full-module mode

### Artifacts
- all gate checklists included and actionable as written
- template structure followed; no placeholder sections

### Quality
- trade-offs stated wherever recommendations are context-dependent
- terminology explained on first use
- reads as practitioner-written, not generic AI advice

## Anti-Patterns

Avoid: Cluster-geometry overreading — measuring distances between t-SNE or UMAP clusters, comparing cluster sizes and densities, and reporting embedding-space quantities the algorithms never promised to preserve; Parameter roulette — running defaults once, or worse, sweeping perplexity and n_neighbors until the picture matches the hoped-for story, with no sensitivity documentation; Single-run trust — treating one stochastic embedding as ground truth when seed variation produces materially different layouts, skipping stability assessment entirely; Method monoculture — reaching for t-SNE or UMAP for every problem, ignoring when PCA, MDS, or hierarchical structure better serves the analytic question; Preprocessing amnesia — embedding raw, unscaled, outlier-ridden, or badly normalized data and blaming the algorithm for garbage layouts the inputs guaranteed; exercises without evaluation criteria; modules that stop at inspiration without a single decision the learner can now make better; and any content that overstates certainty beyond what the evidence supports.

## Decision Heuristic

When allocating depth under time or length limits, ask:

1. Where will this audience actually stall in practice?
2. Which unit protects the most value if taught well?
3. Can the learner run the framework tomorrow without me?
4. Is every example doing work a plain sentence could not?
5. What would a skeptical domain expert say about this material?

If two topics compete for space, keep the one that changes a decision.

## Final Principle

The goal is not more content about Dimensionality reduction. The goal is that a practitioner could take this material and perform: Engineer embedding inputs through preprocessing pipelines (cleaning-first doctrine with missing-outlier strategies, scaling-normalization systems with method interactions, transformation decisions for skew and composition, feature-space construction awareness), run preprocessing experiments (variant-comparison protocols, ablation testing, known-structure and domain-marker validation, reporting standards, garbage-in symptom recognition), and avoid raw-data embedding, scale blindness, metric autopilot, k-arbitrariness, hubness unawareness, imputation blindness, distance opacity, and single-pipeline trust failures — with the same evidence discipline the field's best practitioners use. The module must function simultaneously as a learning resource, a practical reference, and a working methodology.

## Bundled References

Read `references/domain-content-map.md` during Phases 3–4 for the unit-by-unit content map: scope statements, teaching bullets, evidence anchors, and trade-off prompts.

Read `references/exercise-and-checklist-library.md` during Phases 5–6 for the ready-to-adapt exercises and the professional gate checklists.

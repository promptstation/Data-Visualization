# STEP 2 — Engineered Prompt for Section 9 (Information Architecture & Visual Hierarchy)

Produced by applying `_pipeline/step1-reusable-prompt.md` to Section 9 of
the Information Architecture & Visual Hierarchy curriculum outlook. This prompt is the task input for Step 4
skill creation (per Skill Building.md).

---

RESULT:

Develop a comprehensive, professional-level learning module on **“Filtering, Search, and Query Interfaces: The Dynamic-Query Layer”** as a core intermediate stage of a curriculum in The art of organizing complex datasets into intuitive visual structures — Shneiderman's mantra, dashboard architecture, progressive disclosure, and navigation systems mastered as the structural backbone of data products. Cover the refinement-interface discipline: the dynamic-query concept — the filter doctrine: the dynamic-query principle (the immediate-continuous-feedback subset manipulation — the Ahlberg-Shneiderman lineage: the filter-adjustments with the instant visual-result updates replacing submit-and-wait querying), the filter-purpose taxonomy (the noise-reduction filtering for focus, the exploration filtering for what-if investigation, the segmentation filtering for group isolation, the search filtering for known-item retrieval), and the filter-as-primary-interface insight (the modern analytics reality: filtering and querying as the dominant interaction mode — the zoom-and-filter mantra beat as the highest-frequency user action demanding the deepest craft) as the doctrinal foundation; the filter-control vocabulary — the mechanism toolkit: the categorical-filters (the checkbox-lists and multi-select patterns with the count-and-search support for long lists, the tag-and-chip selection displays, the hierarchical-category filters — the tree-select and drill-across-category patterns), the range-filters (the slider-and-input hybrids for numeric ranges — the dual-handle conventions, the histogram-backed-range-sliders showing distribution context — the filter-with-feedback pattern, the date-range-picker systems with the relative-and-absolute modes), the toggle-and-segment filters (the boolean and small-set switchers, the segmented-control granularity selectors), the spatial-and-visual filters (the map-viewport-and-region filtering from the spatial discipline, the brush-on-chart filtering — the timeline-and-scatter brush subset selection, the lasso-and-rectangle visual queries), and the text-search filters (the keyword filtering within result sets, the fuzzy-and-typed search for dimension values) as the control vocabulary; the filter-architecture patterns — the system design: the filter-panel-arrangements (the sidebar-filter-rail conventions, the top-filter-bar patterns, the in-context-and-inline filters beside result displays, the filter-placement-by-frequency logic — the primary-filters-always-visible from the dashboard discipline), the filter-state-management (the active-filter-summary displays — the applied-filter chip-rows with individual-removal, the filter-state-persistence across navigation and sessions, the reset-and-clear ergonomics — the clear-all and single-removal patterns), the filter-logic-systems (the AND-within-dimension-OR-across conventions — the boolean-composition clarity and user-mental-model matching, the filter-conflict-and-empty-result handling — the no-results recovery experiences with the filter-relaxation suggestions), the dependent-and-cascading filters (the dimension-value-narrowing by prior selections — the cascading-availability patterns, the circular-dependency avoidance), and the filter-performance-engineering (the sub-100ms-feedback requirement — the client-side-index-filtering for instant response from the latency discipline, the server-query-debouncing and progressive-result patterns for heavy filters, the result-count-preview computation strategies) as the architecture system; the search-interface-design — the query layer: the search-placement-and-types (the global-search versus scoped-search distinction, the command-palette patterns for power users — the keyboard-first navigation-and-action search), the search-result-design (the result-typing and grouping — the mixed-entity result sections, the result-snippet-and-context display with the match-highlighting, the result-ranking-and-relevance transparency at awareness), the query-refinement-loops (the search-then-filter hybrids — the faceted-refinement after query, the related-and-suggested-query patterns), the search-analytics-driven-improvement (the query-log-analysis — the no-result-and-low-click query identification, the synonym-and-terminology-gap discovery feeding the labeling systems), and the empty-and-error-state-craft (the no-match recovery experiences — the spelling-suggestions, scope-broadening options, and browse-fallbacks) as the search system; the faceted-navigation-patterns — the structured-exploration: the facet-system-design (the dimension-based-facet selection — the which-attributes-deserve-facets decision by usage-and-cardinality, the facet-value-ordering — the count-versus-alphabetical-versus-logical ordering, the facet-count-display — the result-size signaling per value), the facet-interaction-mechanics (the multi-facet-AND and intra-facet-OR conventions, the facet-state visibility-and-removal, the progressive-facet-narrowing with the count-updates), the facet-overflow-management (the show-more-and-scroll patterns for long facet lists, the facet-search within dimensions, the top-N-with-expansion conventions), and the faceted-search-integration (the query-plus-facet combined refinement — the e-commerce-and-analytics-catalog patterns) as the faceted system; the query-building-interfaces — the advanced-layer: the visual-query-builders (the condition-row interfaces — the field-operator-value compositions with the AND-OR grouping controls, the natural-language-adjacent phrasing patterns for readability), the saved-query-and-view-systems (the filter-configuration persistence — the named-view saving and sharing, the personal-and-team-view-libraries, the view-versioning-and-defaults management), the query-history-and-progressive-refinement (the interaction-history support from the mantra tasks — the undo-and-replay of filter sequences, the recent-query accelerators), and the power-user-accelerators (the query-language-and-expression interfaces at awareness — the filter-syntax support for complex conditions, the keyboard-driven-filter workflows) as the advanced-query tier; the filter-and-result-feedback-design — the response-craft: the immediate-result-feedback patterns (the live-count-updates per filter-adjustment, the result-visualization updates — the chart-and-table re-rendering on filter change with the transition-continuity from the animation disciplines, the partial-and-empty-result-state communication), the filter-effect-explanation (the what-changed signaling — the result-diff highlights and count-delta indicators, the filtered-versus-total-context display — the part-of-whole visibility maintaining the overview sense), the progressive-result-loading (the large-result-set handling — the pagination-virtualization-and-infinite-scroll integration with the filter-responsiveness), and the feedback-latency-honesty (the processing-state indication for slow queries, the stale-result-and-updating-state clarity) as the feedback system; the filter-usability-engineering — the human-factors layer: the filter-mental-model-matching (the user-logic-versus-system-logic alignment — the boolean-comprehension testing, the filter-behavior-predictability discipline), the choice-overload-management (the filter-option-curation — the high-value-dimension prioritization, the progressive-filter-disclosure from section 6 — the advanced-filters-behind-expansion), the filter-discoverability (the hidden-filter-capability revelation — the filter-affordance-signaling, the empty-state-filter-suggestions), the accessibility-of-filter-interfaces (the keyboard-operable-controls, the screen-reader-state-announcements for applied-filters-and-results — the live-region integration, the focus-management-across-filter-result-updates), and the filter-error-prevention (the destructive-filter-confirmation patterns, the accidental-clear protection, the filter-state-loss-prevention on navigation) as the usability engineering; the analytics-of-filtering — the evidence-loop: the filter-usage-telemetry (the filter-interaction-logging — the dimension-usage-frequency, sequence-and-combination patterns, the abandonment-and-reset signals), the filter-effectiveness-measurement (the task-completion-with-filters metrics, the time-to-target-subset measurement, the no-result-and-zero-value-filter identification), the A-B-and-iterative-filter-optimization (the placement-label-and-default experiments, the facet-ordering-and-count-display variants), and the filter-driven-content-decisions (the usage-evidence informing dimension-and-control curation — the remove-unused-add-missing discipline) as the optimization system; the filter-failure-modes — the diagnostic library: the filter-maze failure (the control-complexity-overload from the dashboard anti-patterns — the too-many-filters-too-many-options collapse), the invisible-state failure (the active-filters-unseen disorientation — the mystery-subset confusion), the dead-end-filter failure (the empty-results-without-recovery traps), the performance-collapse failure (the filter-adjustment-latency destroying the dynamic-query principle), and the mental-model-mismatch failure (the boolean-logic-surprise confusion — the OR-where-AND-expected errors) with the systematic-repair-patterns as the diagnostic discipline; the domain-filter-patterns — the applied-library: the temporal-filtering-systems (the time-window-and-comparison-period controls, the seasonality-and-custom-calendar handling), the geographic-filtering (the region-hierarchy-and-map-viewport filters from the spatial discipline), the hierarchical-dimension-filtering (the org-product-and-category-tree navigation-filters), and the entity-relationship-filtering (the related-entity-subsetting — the customers-with-orders-with-products chains) as the domain-patterns; and the filtering-and-query deliverable — the dynamic-query interface architecture with feedback, usability, and optimization engineering. The module should assume the learner is progressing from competent beginner toward expert practitioner, and should function simultaneously as a learning resource, a practical reference, and a working methodology.

Convert each of the following capabilities the learner must gain into explicit “Teach how to” treatment with a practical framework the learner could use on a real project, not merely theory:

* Teach how to architect dynamic-query interfaces on the immediate-feedback filtering doctrine across the control vocabulary (categorical multi-selects, histogram-backed range sliders, toggles, spatial-visual brushes, text search) with placement-by-frequency panels, visible persistent filter state, boolean-logic mental-model matching, cascading dependencies, and sub-100ms performance engineering.

* Teach how to design search interfaces (global-scoped-command-palette, typed grouped results, refinement loops, query-log-driven improvement, recovery states).

* Teach how to build faceted navigation with dimension selection, ordering, overflow management, and query integration, provide advanced query builders, saved views, history replay, and power-user accelerators, craft result feedback (live counts, effect explanation, part-of-whole context, latency honesty), engineer usability through overload management, discoverability, accessibility announcements, and error prevention.

* Teach how to optimize via filter telemetry and experiments, repair maze-invisible-state-dead-end-performance-mismatch failures.

* Teach how to apply temporal, geographic, hierarchical, and relational domain patterns.

Use numerous realistic examples throughout the module, drawn from professional practice in The art of organizing complex datasets into intuitive visual structures — Shneiderman's mantra, dashboard architecture, progressive disclosure, and navigation systems mastered as the structural backbone of data products. For each important principle, show weak, improved, and professional-level versions of the same work where useful.

Include practical exercises that require the learner to:

1. Architect dynamic-query interfaces on the immediate-feedback filtering doctrine across the control vocabulary (categorical multi-selects, histogram-backed range sliders, toggles, spatial-visual brushes, text search) with placement-by-frequency panels, visible persistent filter state, boolean-logic mental-model matching, cascading dependencies, and sub-100ms performance engineering, applied to a realistic scenario provided with the exercise.

2. Design search interfaces (global-scoped-command-palette, typed grouped results, refinement loops, query-log-driven improvement, recovery states), applied to a realistic scenario provided with the exercise.

3. Build faceted navigation with dimension selection, ordering, overflow management, and query integration, provide advanced query builders, saved views, history replay, and power-user accelerators, craft result feedback (live counts, effect explanation, part-of-whole context, latency honesty), engineer usability through overload management, discoverability, accessibility announcements, and error prevention, applied to a realistic scenario provided with the exercise.

4. Optimize via filter telemetry and experiments, repair maze-invisible-state-dead-end-performance-mismatch failures, applied to a realistic scenario provided with the exercise.

5. Apply temporal, geographic, hierarchical, and relational domain patterns, applied to a realistic scenario provided with the exercise.

6. Integrate the full section: take one realistic problem in IA and hierarchy through every capability above as a coherent capstone deliverable.

For each exercise, provide the scenario, objective, constraints, expected deliverable, evaluation criteria, and an expert-quality example solution where appropriate.

Structure the material progressively: establish the fundamental concepts and distinctions of this section first, then move toward advanced professional application, showing how the concepts interact only after each is clear on its own.

Use professional terminology from The art of organizing complex datasets into intuitive visual structures — Shneiderman's mantra, dashboard architecture, progressive disclosure, and navigation systems mastered as the structural backbone of data products where relevant, but explain each specialized term in clear language on first use. Do not make the material sound like generic AI-generated advice; it should read as if written by an experienced practitioner.

Research the subject using high-quality professional and academic sources. Prioritize authoritative sources such as Shneiderman's Visual Information-Seeking Mantra and the eight golden tasks of interface design for dynamic visual presentations, with the Plaisant lineage extensions; The Rosenfeld-Arango information-architecture canon (organization, labeling, navigation, and search systems) applied to data products; Few's dashboard design doctrine (Information Dashboard Design and Now You See It lineage) on glanceability, layout, and analytic arrangement; The visual-hierarchy tradition from graphic design (the Lohr-lineage and Tufte layout craft, proximity-alignment-contrast principles) integrated with the Gestalt literature; Nielsen Norman Group research on progressive disclosure, navigation, and analytics-UX usability findings; The card-sorting and IA evaluation methods literature (tree testing, first-click studies, wayfinding research); Design-systems practice for data products (the enterprise dashboard-system and component-library documentation traditions). Avoid relying on low-quality SEO articles, content farms, or unsupported “best practice” claims; distinguish established research findings from professional conventions and informed recommendations, and never invent statistics or citations.

Where a recommendation depends on context, explicitly explain the trade-off rather than presenting it as an absolute rule.

End the module with actionable professional checklists — a architect-dynamic-query-interfaces checklist, a design-search-interfaces-global checklist, a build-faceted-navigation-with checklist, a optimize-via-filter-telemetry checklist, a apply-temporal-geographic-hierarchical checklist — that a practitioner could actually use before real-world delivery.

The final result should be comprehensive enough to serve as an advanced professional training module, organized clearly enough that a learner can study it progressively and apply each concept in practical work.

# STEP 2 — Engineered Prompt for Section 7 (Information Architecture & Visual Hierarchy)

Produced by applying `_pipeline/step1-reusable-prompt.md` to Section 7 of
the Information Architecture & Visual Hierarchy curriculum outlook. This prompt is the task input for Step 4
skill creation (per Skill Building.md).

---

RESULT:

Develop a comprehensive, professional-level learning module on **“Navigation, Wayfinding, and Orientation Systems”** as a core intermediate stage of a curriculum in The art of organizing complex datasets into intuitive visual structures — Shneiderman's mantra, dashboard architecture, progressive disclosure, and navigation systems mastered as the structural backbone of data products. Cover the movement-and-orientation engineering: the navigation-systems taxonomy — the movement architecture: the global-navigation patterns (the persistent top-level structure — the primary-menu, sidebar, and tab-bar architectures with the data-product adaptations: the workspace-and-view switchers), the local-navigation patterns (the within-section movement — the sub-menus, in-page anchors, and panel switching, the context-scoped navigation state), the contextual-navigation (the inline links and related-content paths — the drill-across and cross-reference movement, the breadcrumb-adjacent lateral navigation), the utility-navigation (the search, settings, help, and account access — the supporting-movement layer), and the navigation-hierarchy design (the depth-and-breadth trade-off — the wide-shallow versus narrow-deep structures: the breadth-favoring evidence for wayfinding, the seven-plus-minus grouping guidance at each level) as the movement architecture; the wayfinding-system design — the orientation engineering: the wayfinding-question framework (the where-am-I, where-can-I-go, how-do-I-get-there, how-do-I-get-back orientation questions — the systematic answer provision), the position-indication patterns (the active-state highlighting in menus and tabs, the breadcrumb trails with the hierarchy-path visibility, the page-and-section titles matching navigation labels — the label-consistency discipline), the landmark-and-reference-point design (the persistent-chrome elements anchoring orientation — the header, sidebar, and footer continuity, the visual-landmark distinctiveness per section), the map-and-index provisions (the sitemap-and-overview access, the search-as-wayfinding-recovery, the A-Z-and-catalog indexes for large content sets), and the transition-orientation support (the page-and-view-change continuity cues — the preserved-chrome and animated-transitions maintaining spatial context, the scroll-position-and-state restoration on return) as the orientation system; the information-scent engineering — the path-signaling craft: the scent-source design (the link-and-label wording predicting destination content — the descriptive-over-clever discipline, the partial-content previews and metadata signals — the thumbnails, counts, and date indicators), the scent-strength calibration (the high-scent primary paths versus ambient secondary options — the progressive-scent reduction with depth, the scent-consistency across navigation surfaces), the scent-for-data-products specifics (the metric-and-dimension naming as scent — the terminology-matching to user vocabulary from the labeling-systems tradition, the data-freshness and count signals in navigation items), and the scent-verification methods (the first-click testing for label effectiveness, the navigation-path analysis from telemetry — the expected-versus-actual path comparison) as the signaling craft; the navigation-patterns for analytic structures — the data-product specifics: the workspace-and-project navigation (the multi-analysis workspace switching — the saved-view and project-organization patterns, the recent-and-favorite access accelerators), the view-sequence navigation (the guided-analysis flows — the overview-to-detail-to-related progressions with the state carrying forward, the free-exploration versus structured-path balancing), the dashboard-suite navigation (the cross-dashboard movement with the filter-and-context propagation from section 5, the role-based navigation defaults), the data-entity navigation (the record-and-dimension movement — the entity-page hierarchies: the customer-product-region drill structures, the related-entity lateral links), and the temporal-navigation (the time-range and period movement — the previous-next-period conventions, the calendar-and-timeline navigation integration) as the analytic-navigation library; the search-as-navigation — the query movement: the search-placement-and-prominence (the global-search positioning conventions, the search-versus-browse balance by user-knowledge state — the known-item versus exploratory split from the mantra extensions), the search-scope-systems (the scoped-and-filtered search — the within-section and type-filtered queries, the faceted-search-result navigation with the refinement loops), the search-result-wayfinding (the result-context display — the location-and-hierarchy indication in results, the result-to-destination continuity), the autocomplete-and-suggestion navigation (the typeahead-as-shortcut patterns, the recent-and-popular accelerators), and the no-result-and-partial-result recovery (the suggestion-and-alternative provisions, the browse-fallback guidance, the search-analytics-driven improvement loops) as the query-navigation system; the deep-linking-and-state-navigation — the shareable movement: the URL-as-navigation-state architecture (the view, filter, and selection state encoded in URLs — the shareable-and-bookmarkable analysis states, the state-serialization discipline from the interaction frameworks), the deep-link-entry experiences (the cold-start orientation for link-arriving users — the context-reconstruction and wayfinding-restoration patterns, the landing-in-detail disorientation prevention), the browser-history integration (the back-and-forward button correctness — the history-state management for SPA data products, the navigation-interruption handling), the link-expiration-and-permission realities (the stale-link and access-denied recovery experiences), and the embedded-navigation contexts (the iframe-and-embed navigation constraints, the cross-application deep-linking patterns) as the state-navigation engineering; the mobile-and-constrained navigation — the adaptive movement: the mobile-navigation patterns (the bottom-tab and hamburger-menu architectures — the thumb-zone ergonomics, the progressive-menu disclosure, the gesture-navigation — the swipe-back and pull-refresh conventions), the constrained-context adaptations (the small-screen navigation simplification — the priority-based menu curation, the watch-and-kiosk navigation extremes at awareness), the touch-target-and-density engineering (the tap-target-size requirements, the navigation-density reduction for touch), and the responsive-navigation-transitions (the desktop-to-mobile navigation transformations — the sidebar-to-drawer and menu-collapse patterns with the state-preservation continuity) as the adaptive-navigation craft; the accessible-navigation — the inclusive movement: the keyboard-navigation architecture (the logical-tab-order engineering — the DOM-order-matches-visual-order discipline, the skip-link provisions for chrome bypass, the focus-trapping and management in overlays and menus, the keyboard-shortcut systems with the discoverability and conflict management), the screen-reader-navigation support (the landmark-regions and heading-structure navigation — the ARIA-landmark semantics, the navigation-state announcements — the current-page and expanded-collapsed communications), the cognitive-accessibility of navigation (the consistent-and-predictable structures, the plain-language labels, the orientation-support generosity), and the navigation-testing-with-assistive-technology (the keyboard-path and screen-reader-journey audits from the accessibility discipline) as the inclusive-navigation system; the navigation-analytics-and-optimization — the evidence loop: the navigation-telemetry instrumentation (the path-and-flow analytics — the view-sequence tracking, the navigation-element interaction rates, the search-query and result-click logging), the wayfinding-failure detection (the lost-user signals — the rapid-back-button, dead-end, and orphan-page patterns, the search-rescue frequency as the navigation-failure metric, the rage-click and wandering-session detection concepts), the A-B-and-iterative navigation optimization (the label, placement, and structure experiments — the task-success and time-to-destination metrics), and the persona-flow validation (the representative-journey testing per audience segment — the navigation-fit assessment) as the optimization system; the navigation-failure-modes — the diagnostic library: the maze failure (the disorienting-structure collapse — the deep-narrow hierarchies without landmarks or recovery, the dead-end accumulation), the label-failure (the terminology-mismatch scent collapse — the internal-jargon navigation labels, the ambiguous-and-overlapping categories), the state-loss failure (the context-destruction on movement — the filter-and-scroll resets, the back-button-breakage traps), the navigation-proliferation failure (the redundant-and-competing navigation systems — the multiple-menus-fighting confusion), and the mobile-navigation-afterthought failure (the desktop-structure-squeezed collapse) with the systematic-repair patterns as the diagnostic discipline; and the navigation deliverable — the orientation-complete, scent-rich, accessible, telemetry-optimized movement system. The module should assume the learner is progressing from competent beginner toward expert practitioner, and should function simultaneously as a learning resource, a practical reference, and a working methodology.

Convert each of the following capabilities the learner must gain into explicit “Teach how to” treatment with a practical framework the learner could use on a real project, not merely theory:

* Teach how to architect navigation systems (global-local-contextual-utility layers with breadth-favoring depth-breadth trade-offs) and wayfinding answering the four orientation questions through position indication, landmarks, maps-indexes, and transition continuity, engineer information scent via predictive labeling, previews, data-product terminology matching, and first-click verification.

* Teach how to build analytic navigation patterns (workspaces, view sequences, dashboard suites, entity hierarchies, temporal movement).

* Teach how to integrate search-as-navigation with scoped facets and no-result recovery.

* Teach how to implement deep-linking state navigation with cold-start orientation and browser-history correctness.

* Teach how to adapt to mobile-constrained contexts with thumb-zone patterns and responsive transformations, ensure keyboard-screen-reader-cognitive accessibility with AT auditing.

* Teach how to optimize through path telemetry, lost-user detection, and A-B experiments, and repair maze-label-state-loss-proliferation failures systematically.

Use numerous realistic examples throughout the module, drawn from professional practice in The art of organizing complex datasets into intuitive visual structures — Shneiderman's mantra, dashboard architecture, progressive disclosure, and navigation systems mastered as the structural backbone of data products. For each important principle, show weak, improved, and professional-level versions of the same work where useful.

Include practical exercises that require the learner to:

1. Architect navigation systems (global-local-contextual-utility layers with breadth-favoring depth-breadth trade-offs) and wayfinding answering the four orientation questions through position indication, landmarks, maps-indexes, and transition continuity, engineer information scent via predictive labeling, previews, data-product terminology matching, and first-click verification, applied to a realistic scenario provided with the exercise.

2. Build analytic navigation patterns (workspaces, view sequences, dashboard suites, entity hierarchies, temporal movement), applied to a realistic scenario provided with the exercise.

3. Integrate search-as-navigation with scoped facets and no-result recovery, applied to a realistic scenario provided with the exercise.

4. Implement deep-linking state navigation with cold-start orientation and browser-history correctness, applied to a realistic scenario provided with the exercise.

5. Adapt to mobile-constrained contexts with thumb-zone patterns and responsive transformations, ensure keyboard-screen-reader-cognitive accessibility with AT auditing, applied to a realistic scenario provided with the exercise.

6. Optimize through path telemetry, lost-user detection, and A-B experiments, and repair maze-label-state-loss-proliferation failures systematically, applied to a realistic scenario provided with the exercise.

7. Integrate the full section: take one realistic problem in IA and hierarchy through every capability above as a coherent capstone deliverable.

For each exercise, provide the scenario, objective, constraints, expected deliverable, evaluation criteria, and an expert-quality example solution where appropriate.

Structure the material progressively: establish the fundamental concepts and distinctions of this section first, then move toward advanced professional application, showing how the concepts interact only after each is clear on its own.

Use professional terminology from The art of organizing complex datasets into intuitive visual structures — Shneiderman's mantra, dashboard architecture, progressive disclosure, and navigation systems mastered as the structural backbone of data products where relevant, but explain each specialized term in clear language on first use. Do not make the material sound like generic AI-generated advice; it should read as if written by an experienced practitioner.

Research the subject using high-quality professional and academic sources. Prioritize authoritative sources such as Shneiderman's Visual Information-Seeking Mantra and the eight golden tasks of interface design for dynamic visual presentations, with the Plaisant lineage extensions; The Rosenfeld-Arango information-architecture canon (organization, labeling, navigation, and search systems) applied to data products; Few's dashboard design doctrine (Information Dashboard Design and Now You See It lineage) on glanceability, layout, and analytic arrangement; The visual-hierarchy tradition from graphic design (the Lohr-lineage and Tufte layout craft, proximity-alignment-contrast principles) integrated with the Gestalt literature; Nielsen Norman Group research on progressive disclosure, navigation, and analytics-UX usability findings; The card-sorting and IA evaluation methods literature (tree testing, first-click studies, wayfinding research); Design-systems practice for data products (the enterprise dashboard-system and component-library documentation traditions). Avoid relying on low-quality SEO articles, content farms, or unsupported “best practice” claims; distinguish established research findings from professional conventions and informed recommendations, and never invent statistics or citations.

Where a recommendation depends on context, explicitly explain the trade-off rather than presenting it as an absolute rule.

End the module with actionable professional checklists — a architect-navigation-systems-global checklist, a build-analytic-navigation-patterns checklist, a integrate-search-as-navigation checklist, a implement-deep-linking-state checklist, a adapt-to-mobile-constrained checklist — that a practitioner could actually use before real-world delivery.

The final result should be comprehensive enough to serve as an advanced professional training module, organized clearly enough that a learner can study it progressively and apply each concept in practical work.

# STEP 2 — Engineered Prompt for Section 7 (Preattentive Processing)

Produced by applying `_pipeline/step1-reusable-prompt.md` to Section 7 of
the Preattentive Processing curriculum outlook. This prompt is the task input for Step 4
skill creation (per Skill Building.md).

---

RESULT:

Develop a comprehensive, professional-level learning module on **“Measuring Preattention: The Visual Search Paradigm and Search-Slope Literacy”** as a core intermediate stage of a curriculum in The science of visual attributes (color hue, size, orientation, motion) processed in milliseconds before conscious attention — engineered to direct the viewer's eye instantly in data visualization. Build the measurement discipline: the search-paradigm core — the instrument: the target-detection experiment formalized (the display of distractor items with or without a target differing by the tested feature, the response-time and accuracy measurement across set sizes — the display-item-count manipulation) as the standard preattentivity assay inherited from the feature-integration research program; the search-slope metric — the verdict scale: the RT-by-set-size regression slope interpretation (the flat-slope criterion — the documented roughly-under-10-ms-per-item threshold region indicating parallel pop-out processing, the steep-slope serial-search signature at 25-60+ ms per item, the intermediate guided-search slopes) making preattentivity a measured continuum rather than a binary label; the presence-absence asymmetry design — the protocol detail: the target-present versus target-absent trial analysis (the absent-trial slopes typically double the present-trial slopes under serial search — the exhaustive-search signature, the parallel-search equivalence) as the diagnostic pair distinguishing processing modes; the singleton-detection paradigm — the complementary assay: the odd-one-out displays measuring the feature-difference detection without set-size search (the reaction-time-by-contrast-magnitude curves — the difference-threshold mapping) quantifying how much contrast a channel needs to pop reliably — the salience-calibration instrument; the feature-versus-conjunction comparison — the classic design: the within-experiment contrast (the same elements tested as single-feature targets versus conjunction targets — the slope divergence demonstrating the integration cost) as the teaching-and-verification protocol for design teams; building practical search tests — the designer's workflow: the display-capture preparation (screenshotting real dashboard states at realistic densities), the participant protocol (the naive-viewer recruitment from course 1, the trial-count and randomization requirements for stable slopes, the practice-trial conventions), the low-cost tooling (the browser-based experiment frameworks — the jsPsych-PsychoPy-lineage tools, the timed-response web forms as the minimum viable apparatus) making search testing a designer-runnable method; the threshold-finding applications — the calibration use: the difference-threshold measurement for design parameters (how much size increase makes the key symbol pop? how much saturation drop dims the background sufficiently? — the parameter-tuning experiments), the palette-salience ranking (the candidate color sets tested for singleton-detection speed — the evidence-based palette selection), and the density-tolerance mapping (the clutter level at which the emphasis channel degrades — the operating-envelope measurement); the accuracy-RT complement — the dual-measure discipline: the speed-accuracy trade-off awareness (the RT-only readings missing accuracy costs — the false-alarm and miss-rate measurement, the signal-detection-theory framing — d-prime and criterion as the sensitivity measures separating sensitivity from response bias) upgrading the naive RT analysis; the eye-tracking integration — the gaze complement: the search-task gaze patterns (the first-fixation latency to target as the direct capture measure, the scanpath-efficiency analysis — the path length to detection, the distractor-fixation counts as the competition measure) combining the course-1 eye-tracking methods with search paradigms for mechanism-level diagnosis; the field-versus-lab tension — the validity balance: the controlled-search artificiality (simplified displays, single tasks, laboratory viewing) versus the real-dashboard complexity (multiple tasks, prior knowledge, motivated viewers — the guided-search top-down factor), with the hybrid strategy (lab tests for channel-parameter calibration, field A/B tests and telemetry for in-context validation — the two-tier evidence system); the benchmark literature — the reference values: the documented published search-slope values for the standard features (the orientation, color, size, motion benchmarks from the experimental literature) as the prior-expectation table reducing routine local testing, with the transfer-caution (benchmark displays differ from dense real charts — the local-verification requirement for high-stakes emphasis); the measurement anti-patterns — the methodology pitfalls: the set-size-too-small error (slopes unreliable below the item-count thresholds), the practice-effect contamination (learning curves inflating early-trial RTs — the warm-up and block-order controls), the display-calibration neglect (screen brightness and color variance across participant devices in remote testing — the uncontrolled-monitor problem), and the slope-obsession (ignoring intercept and accuracy — the full-curve reading discipline); and the search-measurement deliverable — the designer-run search-testing capability producing slopes, thresholds, and calibration curves for salience decisions. The module should assume the learner is progressing from competent beginner toward expert practitioner, and should function simultaneously as a learning resource, a practical reference, and a working methodology.

Convert each of the following capabilities the learner must gain into explicit “Teach how to” treatment with a practical framework the learner could use on a real project, not merely theory:

* Teach how to run visual-search assays (target-detection across set sizes, presence-absence asymmetry, singleton-detection thresholds) interpreting search-slope continua (flat-parallel versus steep-serial signatures).

* Teach how to build practical designer-run tests with browser experiment tooling, naive participants, and randomized protocols, calibrate design parameters (size deltas, saturation drops, palette rankings, density tolerances) through difference-threshold measurement.

* Teach how to integrate signal-detection accuracy measures and eye-tracking first-fixation analysis, and balance lab calibration with field validation using benchmark priors while avoiding set-size, practice-effect, calibration, and slope-obsession pitfalls.

Use numerous realistic examples throughout the module, drawn from professional practice in The science of visual attributes (color hue, size, orientation, motion) processed in milliseconds before conscious attention — engineered to direct the viewer's eye instantly in data visualization. For each important principle, show weak, improved, and professional-level versions of the same work where useful.

Include practical exercises that require the learner to:

1. Run visual-search assays (target-detection across set sizes, presence-absence asymmetry, singleton-detection thresholds) interpreting search-slope continua (flat-parallel versus steep-serial signatures), applied to a realistic scenario provided with the exercise.

2. Build practical designer-run tests with browser experiment tooling, naive participants, and randomized protocols, calibrate design parameters (size deltas, saturation drops, palette rankings, density tolerances) through difference-threshold measurement, applied to a realistic scenario provided with the exercise.

3. Integrate signal-detection accuracy measures and eye-tracking first-fixation analysis, and balance lab calibration with field validation using benchmark priors while avoiding set-size, practice-effect, calibration, and slope-obsession pitfalls, applied to a realistic scenario provided with the exercise.

4. Integrate the full section: take one realistic problem in Preattentive processing through every capability above as a coherent capstone deliverable.

For each exercise, provide the scenario, objective, constraints, expected deliverable, evaluation criteria, and an expert-quality example solution where appropriate.

Structure the material progressively: establish the fundamental concepts and distinctions of this section first, then move toward advanced professional application, showing how the concepts interact only after each is clear on its own.

Use professional terminology from The science of visual attributes (color hue, size, orientation, motion) processed in milliseconds before conscious attention — engineered to direct the viewer's eye instantly in data visualization where relevant, but explain each specialized term in clear language on first use. Do not make the material sound like generic AI-generated advice; it should read as if written by an experienced practitioner.

Research the subject using high-quality professional and academic sources. Prioritize authoritative sources such as Treisman's feature-integration theory and the visual-search research program (pop-out, search slopes, conjunction costs) as the measuring framework; Healey's preattentive processing research applied directly to scientific visualization and large-dataset graphics; Wolfe's guided-search model — the hybrid bottom-up salience and top-down task-guidance architecture; Itti-Koch computational saliency lineage — bottom-up salience map models and their visualization applications; Ware, Information Visualization: Perception for Design — the preattentive channel catalog and application canon; The change-detection and change-blindness literature (motion-onset capture, transient detection, flicker paradigms) for temporal preattentives; Cleveland-McGill graphical perception hierarchy with its preattentive versus deliberate decoding distinction as the accuracy complement. Avoid relying on low-quality SEO articles, content farms, or unsupported “best practice” claims; distinguish established research findings from professional conventions and informed recommendations, and never invent statistics or citations.

Where a recommendation depends on context, explicitly explain the trade-off rather than presenting it as an absolute rule.

End the module with actionable professional checklists — a run-visual-search-assays checklist, a build-practical-designer-run checklist, a integrate-signal-detection-accuracy checklist — that a practitioner could actually use before real-world delivery.

The final result should be comprehensive enough to serve as an advanced professional training module, organized clearly enough that a learner can study it progressively and apply each concept in practical work.

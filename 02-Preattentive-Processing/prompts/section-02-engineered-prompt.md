# STEP 2 — Engineered Prompt for Section 2 (Preattentive Processing)

Produced by applying `_pipeline/step1-reusable-prompt.md` to Section 2 of
the Preattentive Processing curriculum outlook. This prompt is the task input for Step 4
skill creation (per Skill Building.md).

---

RESULT:

Develop a comprehensive, professional-level learning module on **“The Visual Architecture: Channels, Parallelism, and the Salience Map”** as the opening foundation of a curriculum in The science of visual attributes (color hue, size, orientation, motion) processed in milliseconds before conscious attention — engineered to direct the viewer's eye instantly in data visualization. Build the mechanism model: the early-vision pipeline — the processing stages: the retinal and LGN preprocessing (the center-surround receptive fields as the first contrast-computation layer — the lateral-inhibition machinery enhancing edges and differences before cortex), the cortical feature extraction (the V1 simple and complex cells as orientation, spatial-frequency, and motion-direction filters — the documented channel-selective neuron populations), and the parallel-channel architecture (multiple feature maps computed simultaneously across the visual field) as the hardware whose structure determines the preattentive channel list; the channel concept formalized — the feature-map logic: each visual attribute processed in its own topographic map (the orientation maps, color-opponent maps, motion maps — the retinotopic organization), with within-channel competition (a singleton in one channel dominating that map) and cross-channel interaction (the combination rules) explaining why single features pop and conjunctions often don't; feature-integration theory — the binding architecture: Treisman's model formalized (the preattentive feature stage registering properties in parallel across maps, the focused-attention stage binding features into objects serially — the illusory-conjunction evidence for the two-stage split: features from unattended objects combining wrongly when attention is overloaded), with the design translation (make the important thing a single-feature difference wherever possible; reserve conjunctions for secondary structure); the salience-map concept — the integration layer: the convergence architecture (the feature maps feeding a combined priority representation — the winner-take-all dynamics selecting the next attention target), the documented neurophysiology at awareness depth (the LIP and superior-colliculus priority-map findings, the Itti-Koch computational saliency model as the engineering analog), and the map's properties (contrast-based, context-dependent, suppressible by top-down task sets per the guided-search model); guided search — the hybrid model: Wolfe's synthesis (bottom-up salience weighting the candidate set while top-down task knowledge — what the viewer is looking for — filters and weights channels, producing the observed intermediate search slopes), making real dashboard reading a guided-search process where both display salience and viewer task-set matter — the design lever being channel choice matched to the viewer's expected question; contrast-relativity principle — the context law: preattentive strength as relative difference not absolute value (the singleton advantage — one red dot among gray pops regardless of which red; the same red dot among rainbow colors does not — the distractor-heterogeneity effect), making salience a composition property (background uniformity as the enabling condition — the quiet-canvas requirement); spatial-frequency and scale structure — the resolution layer: the multi-scale processing architecture (the channel systems analyzing coarse-to-fine spatial frequencies — the low-frequency-first global parse, the coarse-scale salience advantage for large structure) explaining why squint-tests work (the course-1 audit validated by mechanism) and why tiny differences in dense fields fail to pop; the temporal window — the timing structure: the integration-time constants (the documented tens-of-milliseconds windows for feature extraction, the brief-presentation paradigms proving preattentive sufficiency), the masking and persistence effects (the iconic-memory buffer), and the implication for dynamic displays (change rates against integration windows — the section-13 streaming preview); the attentional-blink and refractory dynamics — the serial bottleneck: the documented limits of even parallel processing (the attentional-blink phenomenon — second targets missed within half a second of the first, the refractory periods in rapid detection) informing multi-signal timing (staggering alerts, sequencing highlights — the capture-scheduling craft); the architecture-to-design mapping — the synthesis table: each architectural property translated to a design rule (channels → use single-feature differences; relativity → quiet backgrounds; scales → coarse-structure emphasis; windows → change-rate limits; bottlenecks → alert staggering) as the mechanism-grounded practice bridge; the model-honesty note — the science status: the two-stage picture as productive simplification (the continuous-attention and recurrent-processing critiques, the graded-salience reality versus binary pop-out) with the engineering stance (the models predict well enough to design and test against — the evidence-over-theory-debate discipline); and the architecture deliverable — the channel-map-salience mental model grounding every downstream design decision. The module should assume the learner is progressing from competent beginner toward expert practitioner, and should function simultaneously as a learning resource, a practical reference, and a working methodology.

Convert each of the following capabilities the learner must gain into explicit “Teach how to” treatment with a practical framework the learner could use on a real project, not merely theory:

* Teach how to reason from the early-vision architecture (center-surround contrast, channel-selective feature maps, retinotopic parallelism) to the preattentive channel list.

* Teach how to apply feature-integration theory's single-feature pop-out versus conjunction serial-search logic to emphasis decisions.

* Teach how to operate the salience-map and guided-search models (bottom-up contrast plus top-down task weighting) matching channels to viewer questions, exploit contrast-relativity and multi-scale structure through quiet-canvas and coarse-emphasis rules, and schedule signals against temporal windows and attentional-blink limits within productive model simplification.

Use numerous realistic examples throughout the module, drawn from professional practice in The science of visual attributes (color hue, size, orientation, motion) processed in milliseconds before conscious attention — engineered to direct the viewer's eye instantly in data visualization. For each important principle, show weak, improved, and professional-level versions of the same work where useful.

Include practical exercises that require the learner to:

1. Reason from the early-vision architecture (center-surround contrast, channel-selective feature maps, retinotopic parallelism) to the preattentive channel list, applied to a realistic scenario provided with the exercise.

2. Apply feature-integration theory's single-feature pop-out versus conjunction serial-search logic to emphasis decisions, applied to a realistic scenario provided with the exercise.

3. Operate the salience-map and guided-search models (bottom-up contrast plus top-down task weighting) matching channels to viewer questions, exploit contrast-relativity and multi-scale structure through quiet-canvas and coarse-emphasis rules, and schedule signals against temporal windows and attentional-blink limits within productive model simplification, applied to a realistic scenario provided with the exercise.

4. Integrate the full section: take one realistic problem in Preattentive processing through every capability above as a coherent capstone deliverable.

For each exercise, provide the scenario, objective, constraints, expected deliverable, evaluation criteria, and an expert-quality example solution where appropriate.

Structure the material progressively: establish the fundamental concepts and distinctions of this section first, then move toward advanced professional application, showing how the concepts interact only after each is clear on its own.

Use professional terminology from The science of visual attributes (color hue, size, orientation, motion) processed in milliseconds before conscious attention — engineered to direct the viewer's eye instantly in data visualization where relevant, but explain each specialized term in clear language on first use. Do not make the material sound like generic AI-generated advice; it should read as if written by an experienced practitioner.

Research the subject using high-quality professional and academic sources. Prioritize authoritative sources such as Treisman's feature-integration theory and the visual-search research program (pop-out, search slopes, conjunction costs) as the measuring framework; Healey's preattentive processing research applied directly to scientific visualization and large-dataset graphics; Wolfe's guided-search model — the hybrid bottom-up salience and top-down task-guidance architecture; Itti-Koch computational saliency lineage — bottom-up salience map models and their visualization applications; Ware, Information Visualization: Perception for Design — the preattentive channel catalog and application canon; The change-detection and change-blindness literature (motion-onset capture, transient detection, flicker paradigms) for temporal preattentives; Cleveland-McGill graphical perception hierarchy with its preattentive versus deliberate decoding distinction as the accuracy complement. Avoid relying on low-quality SEO articles, content farms, or unsupported “best practice” claims; distinguish established research findings from professional conventions and informed recommendations, and never invent statistics or citations.

Where a recommendation depends on context, explicitly explain the trade-off rather than presenting it as an absolute rule.

End the module with actionable professional checklists — a reason-from-the-early checklist, a apply-feature-integration-theorys checklist, a operate-the-salience-map checklist — that a practitioner could actually use before real-world delivery.

The final result should be comprehensive enough to serve as an advanced professional training module, organized clearly enough that a learner can study it progressively and apply each concept in practical work.

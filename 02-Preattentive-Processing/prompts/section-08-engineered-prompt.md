# STEP 2 — Engineered Prompt for Section 8 (Preattentive Processing)

Produced by applying `_pipeline/step1-reusable-prompt.md` to Section 8 of
the Preattentive Processing curriculum outlook. This prompt is the task input for Step 4
skill creation (per Skill Building.md).

---

RESULT:

Develop a comprehensive, professional-level learning module on **“Salience Maps and Computational Models: Predicting Where Eyes Go”** as a core intermediate stage of a curriculum in The science of visual attributes (color hue, size, orientation, motion) processed in milliseconds before conscious attention — engineered to direct the viewer's eye instantly in data visualization. Cover the modeling-and-prediction layer: the computational-saliency tradition — the engineering lineage: the Itti-Koch bottom-up saliency model (the multi-scale feature-map extraction, the center-surround differencing, the map normalization and combination into the saliency map, the winner-take-all attention shifting) as the canonical architecture translating the section-2 neuroscience into implementable prediction; the feature-map mechanics — the model internals: the channel decomposition (the intensity, orientation, color implementations mirroring the biological channels, the motion extension for video), the across-scale differencing (the center-surround pyramids computing local contrast at multiple resolutions — the spatial-frequency structure from section 2 operationalized), and the normalization politics (the per-map scaling determining channel weights — the combination-rule sensitivity) as the model anatomy; the bottom-up-only limitation — the scope boundary: the pure-salience models predicting fixation-attractiveness of images without task context (the documented predictive validity for free-viewing — the ROC and NSS metric family, the human-fixation-agreement benchmarks) versus the task-blindness failure (the same image's salience map useless for predicting where a analyst looking for an anomaly will fixate — the guided-search top-down gap) making bottom-up models the attention-floor predictor not the full model; the task-aware extensions — the hybrid models: the relevance-weighted saliency approaches (the task-feature up-weighting — the Wolfe-guided-search computational implementations, the conditional-saliency frameworks), the deep-learning saliency generation (the convolutional and transformer saliency predictors trained on fixation datasets — the documented benchmark improvements and the interpretability opacity), and the visualization-specific models (the chart-saliency research — the models trained on dashboard and chart eye-tracking corpora predicting analyst attention) as the prediction-state-of-art; the practical prediction tooling — the applied layer: the saliency-prediction services and libraries (the available attention-heatmap tools for designs and web pages, the design-tool plugins rendering predicted-salience overlays) with the use-discipline (predictions as audit heuristics — does the intended emphasis region dominate the predicted map? — not as ground truth, the model-error awareness); the salience-competition analysis — the diagnostic application: using models to find unintended competitors (the chrome elements, logos, and images out-salienting the data — the reversed-hierarchy detection, the accidental-salience hotspots from clustering artifacts) as the automated composition audit complementing the course-1 manual squint protocols; the map-based design iteration — the workflow integration: the design-predict-compare-adjust loop (rendering the saliency prediction for candidate layouts, comparing against the intended attention hierarchy, adjusting channel weights — the dim-saturate-resize interventions — and re-predicting) as the rapid pre-user-test screening reducing expensive participant studies to genuine ambiguities; the graph-saliency research frontier — the domain science: the documented visualization-perception research on saliency in charts (the mark-type salience differences, the legend-and-annotation capture costs, the density effects on predicted versus actual attention) as the emerging evidence base with the transfer-caution (most saliency research uses natural photographs — the chart-domain gap requiring local validation); the normative-versus-predictive distinction — the philosophy layer: saliency models describing where eyes go (prediction) versus design theory prescribing where eyes should go (the decision-relevance hierarchy — the normative salience design: making the decision-critical element the salience winner even against the model's natural bias), with the reconciliation practice (when data-important and salience-natural diverge — reposition, re-encode, or accept the guided-search top-down support through task framing and annotation); the ethics-of-prediction layer — the manipulation boundary: the attention-prediction power in persuasive contexts (the advertising and feed-design saliency optimization — the engagement-engineering adjacency) with the analyst-honesty standard (in data visualization, salience serves finding truth in the data, not steering conclusions — the emphasis-integrity principle connecting to the course-1 honesty boundary); the model-limitations summary — the calibration knowledge: the documented failure modes of saliency prediction (the semantic-blindness — meaningful elements not predicted when visually quiet, the text-content ignorance — most models not reading labels, the individual-variance erasure — population-average predictions) as the humility checklist bounding model trust; and the computational-salience deliverable — the prediction-augmented composition audit and iteration workflow with normative-design discipline. The module should assume the learner is progressing from competent beginner toward expert practitioner, and should function simultaneously as a learning resource, a practical reference, and a working methodology.

Convert each of the following capabilities the learner must gain into explicit “Teach how to” treatment with a practical framework the learner could use on a real project, not merely theory:

* Teach how to operate computational saliency models (Itti-Koch architecture, feature-map mechanics, normalization weighting) as attention-floor predictors with bottom-up-only scope awareness.

* Teach how to apply task-aware and deep-learning extensions plus chart-domain research with transfer caution.

* Teach how to use prediction tooling for composition audits detecting unintended competitors and reversed hierarchies.

* Teach how to run design-predict-compare-adjust iteration loops screening layouts before participant studies, and reconcile predictive versus normative salience (decision-relevance overriding natural bias) within emphasis-integrity ethics and model-limitation humility.

Use numerous realistic examples throughout the module, drawn from professional practice in The science of visual attributes (color hue, size, orientation, motion) processed in milliseconds before conscious attention — engineered to direct the viewer's eye instantly in data visualization. For each important principle, show weak, improved, and professional-level versions of the same work where useful.

Include practical exercises that require the learner to:

1. Operate computational saliency models (Itti-Koch architecture, feature-map mechanics, normalization weighting) as attention-floor predictors with bottom-up-only scope awareness, applied to a realistic scenario provided with the exercise.

2. Apply task-aware and deep-learning extensions plus chart-domain research with transfer caution, applied to a realistic scenario provided with the exercise.

3. Use prediction tooling for composition audits detecting unintended competitors and reversed hierarchies, applied to a realistic scenario provided with the exercise.

4. Run design-predict-compare-adjust iteration loops screening layouts before participant studies, and reconcile predictive versus normative salience (decision-relevance overriding natural bias) within emphasis-integrity ethics and model-limitation humility, applied to a realistic scenario provided with the exercise.

5. Integrate the full section: take one realistic problem in Preattentive processing through every capability above as a coherent capstone deliverable.

For each exercise, provide the scenario, objective, constraints, expected deliverable, evaluation criteria, and an expert-quality example solution where appropriate.

Structure the material progressively: establish the fundamental concepts and distinctions of this section first, then move toward advanced professional application, showing how the concepts interact only after each is clear on its own.

Use professional terminology from The science of visual attributes (color hue, size, orientation, motion) processed in milliseconds before conscious attention — engineered to direct the viewer's eye instantly in data visualization where relevant, but explain each specialized term in clear language on first use. Do not make the material sound like generic AI-generated advice; it should read as if written by an experienced practitioner.

Research the subject using high-quality professional and academic sources. Prioritize authoritative sources such as Treisman's feature-integration theory and the visual-search research program (pop-out, search slopes, conjunction costs) as the measuring framework; Healey's preattentive processing research applied directly to scientific visualization and large-dataset graphics; Wolfe's guided-search model — the hybrid bottom-up salience and top-down task-guidance architecture; Itti-Koch computational saliency lineage — bottom-up salience map models and their visualization applications; Ware, Information Visualization: Perception for Design — the preattentive channel catalog and application canon; The change-detection and change-blindness literature (motion-onset capture, transient detection, flicker paradigms) for temporal preattentives; Cleveland-McGill graphical perception hierarchy with its preattentive versus deliberate decoding distinction as the accuracy complement. Avoid relying on low-quality SEO articles, content farms, or unsupported “best practice” claims; distinguish established research findings from professional conventions and informed recommendations, and never invent statistics or citations.

Where a recommendation depends on context, explicitly explain the trade-off rather than presenting it as an absolute rule.

End the module with actionable professional checklists — a operate-computational-saliency-models checklist, a apply-task-aware-and checklist, a use-prediction-tooling-for checklist, a run-design-predict-compare checklist — that a practitioner could actually use before real-world delivery.

The final result should be comprehensive enough to serve as an advanced professional training module, organized clearly enough that a learner can study it progressively and apply each concept in practical work.

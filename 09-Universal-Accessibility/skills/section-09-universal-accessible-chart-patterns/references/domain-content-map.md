# Domain Content Map — Accessible Chart Patterns: Chart-Type-Specific Accessibility Design

Read this during Phase 3 (architecture) and Phase 4 (writing) of the module
development workflow. Verify all factual claims and numbers against the
authoritative sources before publishing (Phase 2 evidence base); flag
anything disputed rather than repeating it.

## Absorbed capabilities this module must produce
1. Apply the five-dimension pattern method (semantics, text alternatives, keyboard model, redundancy, challenges) across chart families: bar-column (table equivalence, ranking narratives, category traversal, value labels, truncation honesty), line-area (trend-inflection-crossing narratives, x-stepping shared tooltips, direct labeling and dash patterns, density aggregation), scatter-bubble (correlation-cluster-outlier summaries, nearest-neighbor stepping, shape-label companions, overplotting alternatives), pie-donut (proportion narratives, percentage-value duals, alternative-recommendation duty), histogram-distribution (shape-skew-modality descriptions, bin stepping, statistical annotations, binning transparency), maps (ranked-region lists, spatial-pattern narratives, region-jump navigation, MAUP honesty, 2D-equivalent mandates), dashboards (region-heading landmarks, priority-ordered summaries, live-region state communication), and specialized families (network topology summaries, sankey flows, matrix grids) with template-based extension to novel types, informed by accessibility-profile chart selection, hybrid representation provision, and pattern-validation discipline against type-specific anti-patterns.

## Table of Contents
1. Unit 1 — Unit 1
2. Capstone integration

---

## Unit 1 — Unit 1

**Scope:** Covers the per-chart-type accessibility craft: the chart-accessibility-pattern-method — the systematic approach: the pattern-template (the per-chart-type accessibility prescription: the semantic-structure, text-alternative content, keyboard-interaction model, color-redundancy strategy, and known-challenges as five dimensions per chart family), the complexity-gradient recognition (the single-series-bar-chart relative simplicity through the multi-series-scatter-and-sankey escalating difficulty — the effort-allocation-by-complexity discipline, the universal-baseline versus type-specific-enhancements architecture), and the pattern-library-maintenance (the living-chart-pattern-catalog in design systems from the governance preview — the tested-pattern accumulation across projects) as the method frame; the bar-and-column-chart patterns — the comparison classics: the semantic structure (the data-table-equivalence advantage — the bar-chart-as-table rendering option with row-per-category semantics, the list-structure alternative for simple series, the group-and-axis labeling for stacked-and-grouped variants), the text-alternative content (the comparison-and-ranking summary tier: the largest-smallest-and-notable-difference narrative, the value-and-category completeness in detailed tiers), the keyboard model (the arrow-key category traversal with value announcement, the group-and-stack navigation for composite bars, the sort-and-filter control access), the redundancy strategy (the value-label direct annotation preference over color-only series distinction, the pattern-fill options for stacked segments, the consistent-order legend pairing), and the known challenges (the truncated-axis-honesty in descriptions: the scale-manipulation disclosure obligations, the many-category-legibility under screen-reader linear traversal: the top-N-and-summary strategies, the stacked-segment-comparison difficulty across all modalities — the alternative-chart recommendations) as the bar pattern; the line-and-area-chart patterns — the trend displays: the semantic structure (the series-as-described-groups with trend-summary emphasis over point-enumeration, the multi-series intersection-and-crossing description obligations), the text-alternative content (the trend-narrative craft: the direction-magnitude-and-inflection-point descriptions — the "rose-steadily-through-Q2-peaked-in-July-then-declined" pattern, the multi-series-relationship summaries: the convergence-divergence-and-crossing narratives, the data-density management: the key-point-selection over exhaustive-value-listing with table-companion for completeness), the keyboard model (the x-position-stepping with multi-series-value announcement — the shared-tooltip keyboard pattern, the series-focus-switching for multi-line charts, the brush-and-zoom time-range keyboard equivalents), the redundancy strategy (the direct-line-labeling superiority for series identification, the dash-pattern-and-marker-shape differentiation for monochrome-and-CVD robustness, the endpoint-value-annotation conventions), and the known challenges (the crossing-line-confusion across all perception modes: the interactive-highlighting-and-small-multiple alternatives, the area-chart-occlusion of-lower-series: the stacking-honesty-and-opacity-considerations, the dense-time-series screen-reader overload: the aggregation-and-summary-first architecture) as the line pattern; the scatter-and-bubble patterns — the relationship displays: the semantic structure (the point-cloud description challenges: the statistical-summary primacy — the correlation-cluster-and-outlier descriptions over point-enumeration, the regression-and-trend-line semantics when present, the density-and-overplotting description obligations), the text-alternative content (the relationship-narrative: the direction-strength-and-exception descriptions — the "strong-positive-relationship-with-three-notable-outliers" pattern, the cluster-and-group-identification for colored-category scatters, the bubble-size-dimension explanation when size encodes), the keyboard model (the Delaunay-nearest-neighbor keyboard stepping from the D3-hover patterns adapted to focus navigation, the quadrant-and-region navigation for large point sets, the filter-and-highlight control access for category exploration), the redundancy strategy (the shape-and-label companions for color-coded categories from the Okabe-Ito discipline, the annotated-outlier-and-notable-point callouts serving all perception modes, the marginal-distribution-companion views), and the known challenges (the overplotting-inaccessibility compounding: the dense-clouds unreadable visually-and-verbally — the aggregation-hexbin-and-density alternatives, the three-encoding-dimension complexity — the size-color-position load management, the correlation-causation-description-honesty) as the scatter pattern; the pie-and-donut-chart patterns — the proportion displays: the semantic structure (the slice-as-list-item semantics with value-and-percentage pairing, the ordered-by-magnitude-versus-legend-order consistency decisions), the text-alternative content (the proportion-narrative: the dominant-share-and-distribution descriptions — the "X-comprises-nearly-half-with-Y-and-Z-splitting-the-remainder" pattern, the percentage-and-absolute-value dual reporting), the keyboard model (the slice-traversal-with-announced-values, the explode-and-highlight interaction equivalents), the redundancy strategy (the direct-label-with-percentage conventions eliminating color-only identification, the consistent-color-to-label ordering), and the known challenges (the angular-comparison-difficulty for-all-users from the encoding-hierarchy traditions — the accessible-alternative recommendation duty: the bar-chart-superiority disclosure, the many-slice-collapse: the top-N-plus-other aggregation-with-honesty, the 3D-pie-prohibition as accessibility-and-honesty alignment) as the pie pattern; the histogram-and-distribution patterns — the shape displays: the semantic structure (the distribution-as-summary primacy: the central-tendency-spread-and-shape semantics over bin-enumeration, the bin-boundary-and-count table equivalence), the text-alternative content (the distribution-narrative: the skew-modality-and-tail descriptions — the "roughly-normal-centered-at-X-with-right-tail-outliers" pattern, the bin-count-completeness in data tiers, the comparison-distribution-relationship descriptions for overlaid histograms), the keyboard model (the bin-stepping-with-range-and-count announcement, the overlay-series-switching), the redundancy strategy (the fill-pattern-and-line-style differentiation for overlaid distributions, the statistical-summary-annotation companions: the mean-median-quartile reference lines with labels), and the known challenges (the binning-choice-transparency: the MAUP-honesty from the aggregation ethics in descriptions, the distribution-shape-verbalization-difficulty: the summary-statistics-plus-notable-features strategy, the violin-and-box-plot-variant-complexity: the five-number-summary verbalization patterns) as the distribution pattern; the map-and-geospatial patterns — the spatial displays: the semantic structure (the geography-as-ranked-list-and-table equivalence from the spatial-equivalents section: the region-value-listings replacing spatial-pattern-reading, the layer-and-control structure semantics for multi-layer maps), the text-alternative content (the spatial-pattern-narrative: the cluster-gradient-and-extreme-region descriptions — the "concentration-in-the-northeast-with-lowest-values-across-the-south" pattern, the choropleth-classification-scheme explanation with MAUP-honesty, the scale-and-projection-context for accurate interpretation), the keyboard model (the region-list-navigation replacing pan-and-zoom exploration from the spatial-interaction discipline, the layer-toggle-and-legend-filter keyboard access, the search-and-jump-to-region command patterns), the redundancy strategy (the pattern-fill-and-label choropleth options for CVD-and-monochrome, the direct-region-labeling for key areas, the legend-class-boundary explicitness), and the known challenges (the spatial-relationship-verbalization-limits: the relative-position-description-conventions and their imprecision honesty, the dense-point-map inaccessibility: the aggregation-and-table-primary strategies from the point-scale discipline, the 3D-globe-and-terrain-exclusion: the 2D-equivalent mandate from the spatial-accessibility obligations) as the map pattern; the dashboard-and-multi-view patterns — the composite displays: the structural semantics (the panel-as-region-with-heading architecture enabling rotor navigation from the landmark discipline, the cross-panel-relationship description obligations: the linked-view-explanation for coordinated dashboards), the navigation-and-orientation (the dashboard-level-summary provision: the overall-status-and-key-metrics-first announcements, the panel-priority-ordering for traversal: the importance-sequenced DOM from the hierarchy discipline, the skip-and-jump provisions for large dashboards), the interaction-coherence (the global-filter-and-control keyboard operation with cross-panel-effect announcement via live regions from the coordination discipline, the drill-down-and-detail keyboard flows with focus-restoration), the state-communication (the filter-and-selection-state-persistent-visibility from the state-transparency systems, the update-and-freshness-announcement for live dashboards: the throttled-live-region discipline), and the known challenges (the dashboard-scale screen-reader fatigue: the summary-and-priority architecture mitigation, the cross-panel-linkage-comprehension: the explicit-relationship-documentation, the real-time-update-overload: the announcement-throttling-and-on-demand-refresh patterns) as the composite pattern; the specialized-chart patterns — the advanced-family guidance: the network-and-hierarchy-visualizations (the topology-summary-primacy: the cluster-hub-and-connection-count descriptions over node-enumeration, the tree-outline-and-edge-list equivalents from the structured-equivalents, the traversal-and-focus keyboard models for graph exploration), the sankey-and-flow-charts (the flow-magnitude-and-path-narratives: the dominant-flow-and-branching descriptions, the stage-table equivalents), the radar-and-polar-charts (the profile-comparison-narratives with axis-value-tables, the angular-position-verbalization challenges and table-primary recommendations), the heatmap-and-matrix-displays (the grid-table-equivalence advantage: the row-column-cell semantics with value announcement, the pattern-and-extreme-summary narratives), and the pattern-extension-method (the five-dimension-template application to novel chart types — the systematic-pattern-derivation skill for emerging visualizations) as the specialized layer; the chart-pattern-selection-and-honesty — the meta-decision layer: the accessibility-informed-chart-selection (the chart-type-accessibility-profiles informing design choices — the bar-over-pie-and-table-over-heatmap guidance where tasks permit, the novel-visualization accessibility-cost accounting in design decisions), the alternative-recommendation-duty (the documenting-when-a-more-accessible-chart-type-would-serve-better — the honest-limitation-communication in accessibility statements), the hybrid-provision-patterns (the multiple-representation-offers: the chart-plus-table-plus-summary standard compositions, the user-representation-preference persistence), and the pattern-evolution-tracking (the emerging-chart-type-pattern-development: the research-and-community-guidance-monitoring from the standards-evolution practice, the pattern-testing-and-validation-cycle integration) as the meta layer; the chart-pattern-anti-patterns — the failure library: the one-size-description (the identical-template-for-all-chart-types — the type-specific-pattern remedy), the point-enumeration-dump (the exhaustive-value-recitation-for-dense-charts — the summary-first-with-table-companion remedy), the legend-color-dependence (the identify-the-red-series-instructions — the direct-label-and-shape remedy), the interaction-visual-only (the hover-revealed-insights-inaccessible-by-keyboard — the focus-equivalent remedy), the description-chart-mismatch (the stale-or-wrong-summary-text — the synchronization-and-generation remedy), the complex-chart-avoidance-cover (the skipping-accessibility-for-hard-chart-types-instead-of-alternatives — the pattern-derivation-and-equivalent remedy), and the pattern-copy-without-validation (the untested-pattern-application-to-new-contexts — the AT-and-user-testing remedy) with detection-methods as the diagnostic discipline; and the chart-patterns deliverable — the chart-family-specific accessible-design pattern library with systematic extension capability.

**What to teach (decompose and expand each bullet into framework-level treatment):**
- Covers the per-chart-type accessibility craft
- the chart-accessibility-pattern-method — the systematic approach: the pattern-template (the per-chart-type accessibility prescription: the semantic-structure, text-alternative content, keyboard-interaction model, color-redundancy strategy, and known-challenges as five dimensions per chart family), the complexity-gradient recognition (the single-series-bar-chart relative simplicity through the multi-series-scatter-and-sankey escalating difficulty — the effort-allocation-by-complexity discipline, the universal-baseline versus type-specific-enhancements architecture), and the pattern-library-maintenance (the living-chart-pattern-catalog in design systems from the governance preview — the tested-pattern accumulation across projects) as the method frame
- the bar-and-column-chart patterns — the comparison classics: the semantic structure (the data-table-equivalence advantage — the bar-chart-as-table rendering option with row-per-category semantics, the list-structure alternative for simple series, the group-and-axis labeling for stacked-and-grouped variants), the text-alternative content (the comparison-and-ranking summary tier: the largest-smallest-and-notable-difference narrative, the value-and-category completeness in detailed tiers), the keyboard model (the arrow-key category traversal with value announcement, the group-and-stack navigation for composite bars, the sort-and-filter control access), the redundancy strategy (the value-label direct annotation preference over color-only series distinction, the pattern-fill options for stacked segments, the consistent-order legend pairing), and the known challenges (the truncated-axis-honesty in descriptions: the scale-manipulation disclosure obligations, the many-category-legibility under screen-reader linear traversal: the top-N-and-summary strategies, the stacked-segment-comparison difficulty across all modalities — the alternative-chart recommendations) as the bar pattern
- the line-and-area-chart patterns — the trend displays: the semantic structure (the series-as-described-groups with trend-summary emphasis over point-enumeration, the multi-series intersection-and-crossing description obligations), the text-alternative content (the trend-narrative craft: the direction-magnitude-and-inflection-point descriptions — the "rose-steadily-through-Q2-peaked-in-July-then-declined" pattern, the multi-series-relationship summaries: the convergence-divergence-and-crossing narratives, the data-density management: the key-point-selection over exhaustive-value-listing with table-companion for completeness), the keyboard model (the x-position-stepping with multi-series-value announcement — the shared-tooltip keyboard pattern, the series-focus-switching for multi-line charts, the brush-and-zoom time-range keyboard equivalents), the redundancy strategy (the direct-line-labeling superiority for series identification, the dash-pattern-and-marker-shape differentiation for monochrome-and-CVD robustness, the endpoint-value-annotation conventions), and the known challenges (the crossing-line-confusion across all perception modes: the interactive-highlighting-and-small-multiple alternatives, the area-chart-occlusion of-lower-series: the stacking-honesty-and-opacity-considerations, the dense-time-series screen-reader overload: the aggregation-and-summary-first architecture) as the line pattern
- the scatter-and-bubble patterns — the relationship displays: the semantic structure (the point-cloud description challenges: the statistical-summary primacy — the correlation-cluster-and-outlier descriptions over point-enumeration, the regression-and-trend-line semantics when present, the density-and-overplotting description obligations), the text-alternative content (the relationship-narrative: the direction-strength-and-exception descriptions — the "strong-positive-relationship-with-three-notable-outliers" pattern, the cluster-and-group-identification for colored-category scatters, the bubble-size-dimension explanation when size encodes), the keyboard model (the Delaunay-nearest-neighbor keyboard stepping from the D3-hover patterns adapted to focus navigation, the quadrant-and-region navigation for large point sets, the filter-and-highlight control access for category exploration), the redundancy strategy (the shape-and-label companions for color-coded categories from the Okabe-Ito discipline, the annotated-outlier-and-notable-point callouts serving all perception modes, the marginal-distribution-companion views), and the known challenges (the overplotting-inaccessibility compounding: the dense-clouds unreadable visually-and-verbally — the aggregation-hexbin-and-density alternatives, the three-encoding-dimension complexity — the size-color-position load management, the correlation-causation-description-honesty) as the scatter pattern
- the pie-and-donut-chart patterns — the proportion displays: the semantic structure (the slice-as-list-item semantics with value-and-percentage pairing, the ordered-by-magnitude-versus-legend-order consistency decisions), the text-alternative content (the proportion-narrative: the dominant-share-and-distribution descriptions — the "X-comprises-nearly-half-with-Y-and-Z-splitting-the-remainder" pattern, the percentage-and-absolute-value dual reporting), the keyboard model (the slice-traversal-with-announced-values, the explode-and-highlight interaction equivalents), the redundancy strategy (the direct-label-with-percentage conventions eliminating color-only identification, the consistent-color-to-label ordering), and the known challenges (the angular-comparison-difficulty for-all-users from the encoding-hierarchy traditions — the accessible-alternative recommendation duty: the bar-chart-superiority disclosure, the many-slice-collapse: the top-N-plus-other aggregation-with-honesty, the 3D-pie-prohibition as accessibility-and-honesty alignment) as the pie pattern
- the histogram-and-distribution patterns — the shape displays: the semantic structure (the distribution-as-summary primacy: the central-tendency-spread-and-shape semantics over bin-enumeration, the bin-boundary-and-count table equivalence), the text-alternative content (the distribution-narrative: the skew-modality-and-tail descriptions — the "roughly-normal-centered-at-X-with-right-tail-outliers" pattern, the bin-count-completeness in data tiers, the comparison-distribution-relationship descriptions for overlaid histograms), the keyboard model (the bin-stepping-with-range-and-count announcement, the overlay-series-switching), the redundancy strategy (the fill-pattern-and-line-style differentiation for overlaid distributions, the statistical-summary-annotation companions: the mean-median-quartile reference lines with labels), and the known challenges (the binning-choice-transparency: the MAUP-honesty from the aggregation ethics in descriptions, the distribution-shape-verbalization-difficulty: the summary-statistics-plus-notable-features strategy, the violin-and-box-plot-variant-complexity: the five-number-summary verbalization patterns) as the distribution pattern
- the map-and-geospatial patterns — the spatial displays: the semantic structure (the geography-as-ranked-list-and-table equivalence from the spatial-equivalents section: the region-value-listings replacing spatial-pattern-reading, the layer-and-control structure semantics for multi-layer maps), the text-alternative content (the spatial-pattern-narrative: the cluster-gradient-and-extreme-region descriptions — the "concentration-in-the-northeast-with-lowest-values-across-the-south" pattern, the choropleth-classification-scheme explanation with MAUP-honesty, the scale-and-projection-context for accurate interpretation), the keyboard model (the region-list-navigation replacing pan-and-zoom exploration from the spatial-interaction discipline, the layer-toggle-and-legend-filter keyboard access, the search-and-jump-to-region command patterns), the redundancy strategy (the pattern-fill-and-label choropleth options for CVD-and-monochrome, the direct-region-labeling for key areas, the legend-class-boundary explicitness), and the known challenges (the spatial-relationship-verbalization-limits: the relative-position-description-conventions and their imprecision honesty, the dense-point-map inaccessibility: the aggregation-and-table-primary strategies from the point-scale discipline, the 3D-globe-and-terrain-exclusion: the 2D-equivalent mandate from the spatial-accessibility obligations) as the map pattern
- the dashboard-and-multi-view patterns — the composite displays: the structural semantics (the panel-as-region-with-heading architecture enabling rotor navigation from the landmark discipline, the cross-panel-relationship description obligations: the linked-view-explanation for coordinated dashboards), the navigation-and-orientation (the dashboard-level-summary provision: the overall-status-and-key-metrics-first announcements, the panel-priority-ordering for traversal: the importance-sequenced DOM from the hierarchy discipline, the skip-and-jump provisions for large dashboards), the interaction-coherence (the global-filter-and-control keyboard operation with cross-panel-effect announcement via live regions from the coordination discipline, the drill-down-and-detail keyboard flows with focus-restoration), the state-communication (the filter-and-selection-state-persistent-visibility from the state-transparency systems, the update-and-freshness-announcement for live dashboards: the throttled-live-region discipline), and the known challenges (the dashboard-scale screen-reader fatigue: the summary-and-priority architecture mitigation, the cross-panel-linkage-comprehension: the explicit-relationship-documentation, the real-time-update-overload: the announcement-throttling-and-on-demand-refresh patterns) as the composite pattern
- the specialized-chart patterns — the advanced-family guidance: the network-and-hierarchy-visualizations (the topology-summary-primacy: the cluster-hub-and-connection-count descriptions over node-enumeration, the tree-outline-and-edge-list equivalents from the structured-equivalents, the traversal-and-focus keyboard models for graph exploration), the sankey-and-flow-charts (the flow-magnitude-and-path-narratives: the dominant-flow-and-branching descriptions, the stage-table equivalents), the radar-and-polar-charts (the profile-comparison-narratives with axis-value-tables, the angular-position-verbalization challenges and table-primary recommendations), the heatmap-and-matrix-displays (the grid-table-equivalence advantage: the row-column-cell semantics with value announcement, the pattern-and-extreme-summary narratives), and the pattern-extension-method (the five-dimension-template application to novel chart types — the systematic-pattern-derivation skill for emerging visualizations) as the specialized layer
- the chart-pattern-selection-and-honesty — the meta-decision layer: the accessibility-informed-chart-selection (the chart-type-accessibility-profiles informing design choices — the bar-over-pie-and-table-over-heatmap guidance where tasks permit, the novel-visualization accessibility-cost accounting in design decisions), the alternative-recommendation-duty (the documenting-when-a-more-accessible-chart-type-would-serve-better — the honest-limitation-communication in accessibility statements), the hybrid-provision-patterns (the multiple-representation-offers: the chart-plus-table-plus-summary standard compositions, the user-representation-preference persistence), and the pattern-evolution-tracking (the emerging-chart-type-pattern-development: the research-and-community-guidance-monitoring from the standards-evolution practice, the pattern-testing-and-validation-cycle integration) as the meta layer
- the chart-pattern-anti-patterns — the failure library: the one-size-description (the identical-template-for-all-chart-types — the type-specific-pattern remedy), the point-enumeration-dump (the exhaustive-value-recitation-for-dense-charts — the summary-first-with-table-companion remedy), the legend-color-dependence (the identify-the-red-series-instructions — the direct-label-and-shape remedy), the interaction-visual-only (the hover-revealed-insights-inaccessible-by-keyboard — the focus-equivalent remedy), the description-chart-mismatch (the stale-or-wrong-summary-text — the synchronization-and-generation remedy), the complex-chart-avoidance-cover (the skipping-accessibility-for-hard-chart-types-instead-of-alternatives — the pattern-derivation-and-equivalent remedy), and the pattern-copy-without-validation (the untested-pattern-application-to-new-contexts — the AT-and-user-testing remedy) with detection-methods as the diagnostic discipline
- and the chart-patterns deliverable — the chart-family-specific accessible-design pattern library with systematic extension capability

**Evidence anchors:** select documented examples and findings from the authoritative sources listed in SKILL.md that illustrate unit 1; grade every claim (established / convention / contested) before teaching it.

**Misconceptions:** identify and correct the most common practitioner misconceptions about unit 1 in Accessibility practice; state the corrected view explicitly.

**Trade-offs to make explicit:** depth vs breadth, rigor vs speed, and the context-dependencies that change the recommendation for unit 1.

## Unit 2 — Capstone integration

**Purpose:** integrate every capability above on one realistic problem end to end.

**Structure:** the learner takes a single problem in Accessibility through each unit's framework in sequence, producing a coherent deliverable that mirrors real professional documentation. Evaluation criteria: internal consistency across artifacts, evidence discipline, honest trade-off statements, and demonstrable use of each unit's framework.

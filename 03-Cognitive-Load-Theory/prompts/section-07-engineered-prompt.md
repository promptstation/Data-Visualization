# STEP 2 — Engineered Prompt for Section 7 (Cognitive Load Theory for Dashboard Design)

Produced by applying `_pipeline/step1-reusable-prompt.md` to Section 7 of
the Cognitive Load Theory for Dashboard Design curriculum outlook. This prompt is the task input for Step 4
skill creation (per Skill Building.md).

---

RESULT:

Develop a comprehensive, professional-level learning module on **“Visual Working Memory in Graphics: Holding Charts in Mind”** as a core intermediate stage of a curriculum in The science of working-memory limits applied to information design — managing intrinsic, extraneous, and germane load so complex dashboards inform users without overwhelming or fatiguing them. Cover the following: specializes the capacity science to visual material: the visuospatial-sketchpad domain — the visual buffer: the working-memory subsystem holding visual forms and spatial arrangements (the documented visual versus spatial dissociation — the shape-holding and location-holding partially separate resources), its refresh mechanisms (the eye-movement cycling — the saccade-back rehearsal, the mental-rotation and transformation costs) and its decay profile as the buffer specification for chart reading; the graphical-object chunk — the visual unit: the research on what counts as one item in visual working memory for graphics (the documented findings: grouped marks held as single objects — the Gestalt-binding capacity effect, the object-based storage — the bar-with-label-with-value as one chunk when bound, the feature-versus-object capacity distinctions) making grouping architecture a capacity architecture; the comparison-in-memory problem — the core dashboard task: the cross-element comparison mechanics (the two-series-hold-and-difference computation, the current-versus-previous evaluation, the multi-panel pattern matching) with the documented comparison costs (the precision degradation with held-item count from section 4, the saccade-distance effects — the far-apart comparisons costing re-encoding, the memory-versus-perception trade-off: comparisons done by looking — the externalized approach — outperforming comparisons done by holding) establishing the alignment-and-proximity comparison design imperative; the externalization principle — the display-as-memory: the systematic strategy of moving holds into the display (the reference lines replacing threshold memory, the ghost-series and prior-period overlays replacing baseline memory, the computed-difference and delta-callouts replacing mental arithmetic, the persistent-selection and pinning tools replacing held-comparisons, the annotation-restatement replacing value memory) as the dominant load-reduction pattern for analytical dashboards; the visual-difficulty factors — the encoding load: the documented decoding costs by visual form (the angle-and-slope judgment difficulty, the area-and-volume estimation errors, the non-aligned-scale comparison costs — the Cleveland-McGill hierarchy read as a load ranking: the accurate-fast encodings are also the low-load encodings), the transformation-load cases (the log-scale reading, the normalized-versus-absolute dual readings, the inverted-axis conventions) requiring schema support or avoidance as the encoding-load map; the pattern-hold capacity — the gestalt-chunk advantage: the trend-shape and cluster-pattern memory (the documented superior retention of meaning-loaded patterns over element lists — the shape-of-the-curve memory, the exception-location memory as compact chunks), with the design enablement (the shape-preserving aspect ratios preventing trend-distortion, the cluster-emphasis annotations naming the patterns — the pattern-labeling chunk-elevation) as the memory-optimizing encoding; the visual-conflict phenomena — the interference cases: the documented visual-working-memory interference (the similar-shape confusion in held items, the location-binding errors under load — the wrong-value-wrong-position swaps, the change-detection failures for held-versus-current comparison — the flicker-task analogs) with the design responses (the discriminable-encoding separation, the position-stability discipline — the reordering animations' binding-cost awareness, the explicit-change-marking removing the hold-and-compare requirement) as the interference management; the multi-view integration load — the cross-panel memory: the coordinated-view comparison costs (the cross-panel value holds, the linked-highlight relief — the selection-propagation externalizing the cross-view binding, the aligned-axis scaffold — the shared-scale position-comparison replacing value-memory) with the panel-count capacity limits (the documented multi-panel comparison degradation — the small-multiple scan strategies as the capacity workaround: sequential single-panel reads against a remembered template) as the multi-view load architecture; the individual visual-memory variance — the audience spread: the documented visual-working-memory capacity differences (the VWM-span individual variation, the field-dependence and visualization-literacy modulation, the aphantasia consideration — the mental-imagery variance affecting externalization dependence) with the conservative-design default (externalize everything — the low-VWM accommodation benefiting all) as the inclusive-capacity practice; the measurement of visual-hold load — the assessment practice: the change-detection and recall probes for displays (the post-glance element-recall as the chunk-capacity measure, the comparison-accuracy-by-distance testing, the dual-task interference protocols — the secondary-task degradation revealing primary-load) with the design-variant application (the externalization interventions measured against hold-heavy baselines) as the visual-memory evidence loop; and the visual-memory deliverable — the externalized, pattern-chunked, interference-managed comparison architecture. The module should assume the learner is progressing from competent beginner toward expert practitioner, and should function simultaneously as a learning resource, a practical reference, and a working methodology.

Convert each of the following capabilities the learner must gain into explicit “Teach how to” treatment with a practical framework the learner could use on a real project, not merely theory:

* Teach how to engineer chart reading around visuospatial working-memory limits using graphical-object chunking (grouping as capacity architecture).

* Teach how to optimize comparison through alignment-proximity design and the externalization principle (reference lines, ghosts, computed deltas, pinning tools).

* Teach how to select low-decoding-load encodings per the accuracy-as-load hierarchy with pattern-chunk elevation (shape-preserving ratios, named clusters).

* Teach how to manage visual interference (similar-shape confusion, binding errors, change-detection failures) through discriminability and stability discipline, accommodate visual-memory variance with externalize-everything defaults.

* Teach how to measure hold-load through recall probes, distance-comparison tests, and dual-task protocols.

Use numerous realistic examples throughout the module, drawn from professional practice in The science of working-memory limits applied to information design — managing intrinsic, extraneous, and germane load so complex dashboards inform users without overwhelming or fatiguing them. For each important principle, show weak, improved, and professional-level versions of the same work where useful.

Include practical exercises that require the learner to:

1. Engineer chart reading around visuospatial working-memory limits using graphical-object chunking (grouping as capacity architecture), applied to a realistic scenario provided with the exercise.

2. Optimize comparison through alignment-proximity design and the externalization principle (reference lines, ghosts, computed deltas, pinning tools), applied to a realistic scenario provided with the exercise.

3. Select low-decoding-load encodings per the accuracy-as-load hierarchy with pattern-chunk elevation (shape-preserving ratios, named clusters), applied to a realistic scenario provided with the exercise.

4. Manage visual interference (similar-shape confusion, binding errors, change-detection failures) through discriminability and stability discipline, accommodate visual-memory variance with externalize-everything defaults, applied to a realistic scenario provided with the exercise.

5. Measure hold-load through recall probes, distance-comparison tests, and dual-task protocols, applied to a realistic scenario provided with the exercise.

6. Integrate the full section: take one realistic problem in Cognitive load through every capability above as a coherent capstone deliverable.

For each exercise, provide the scenario, objective, constraints, expected deliverable, evaluation criteria, and an expert-quality example solution where appropriate.

Structure the material progressively: establish the fundamental concepts and distinctions of this section first, then move toward advanced professional application, showing how the concepts interact only after each is clear on its own.

Use professional terminology from The science of working-memory limits applied to information design — managing intrinsic, extraneous, and germane load so complex dashboards inform users without overwhelming or fatiguing them where relevant, but explain each specialized term in clear language on first use. Do not make the material sound like generic AI-generated advice; it should read as if written by an experienced practitioner.

Research the subject using high-quality professional and academic sources. Prioritize authoritative sources such as Sweller's cognitive load theory — the instructional-design foundation (intrinsic, extraneous, and germane load with the element-interactivity basis); Baddeley's working-memory model (phonological loop, visuospatial sketchpad, central executive) and Cowan's revised capacity findings (the four-chunk limit); Miller's magical number seven lineage with the chunking and recoding tradition; Mayer's multimedia learning principles (coherence, signaling, spatial-contiguity, temporal-contiguity, modality, redundancy) as the experimentally tested load-management toolkit; Paas-van Merrienboer load measurement tradition (subjective rating scales, task-performance triangulation) and NASA-TLX workload assessment; Stephen Few's dashboard design doctrine (glanceability, information density, chart-selection for monitoring) as the applied BI synthesis; Pirolli-Card information foraging theory for navigation-load and scent-following analysis. Avoid relying on low-quality SEO articles, content farms, or unsupported “best practice” claims; distinguish established research findings from professional conventions and informed recommendations, and never invent statistics or citations.

Where a recommendation depends on context, explicitly explain the trade-off rather than presenting it as an absolute rule.

End the module with actionable professional checklists — a engineer-chart-reading-around checklist, a optimize-comparison-through-alignment checklist, a select-low-decoding-load checklist, a manage-visual-interference-similar checklist, a measure-hold-load-through checklist — that a practitioner could actually use before real-world delivery.

The final result should be comprehensive enough to serve as an advanced professional training module, organized clearly enough that a learner can study it progressively and apply each concept in practical work.

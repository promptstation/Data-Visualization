# STEP 2 — Engineered Prompt for Section 10 (Cognitive Load Theory for Dashboard Design)

Produced by applying `_pipeline/step1-reusable-prompt.md` to Section 10 of
the Cognitive Load Theory for Dashboard Design curriculum outlook. This prompt is the task input for Step 4
skill creation (per Skill Building.md).

---

RESULT:

Develop a comprehensive, professional-level learning module on **“Navigation Load and Information Foraging: Finding Without Fatigue”** as a core intermediate stage of a curriculum in The science of working-memory limits applied to information design — managing intrinsic, extraneous, and germane load so complex dashboards inform users without overwhelming or fatiguing them. Cover the wayfinding-cost dimension: information-foraging theory — the navigation model: the Pirolli-Card framework (users as foragers following information scent — the cue-trails suggesting value ahead, with the cost-value economics: navigation paths chosen by expected-information-gain per interaction-cost) as the theory of dashboard and analytics-system wayfinding, making navigation design a scent-engineering discipline; the scent-craft — the cue design: the documented scent strengtheners (the descriptive-over-generic labels — 'Q3 Revenue Exceptions' over 'Details', the preview-snippets and thumbnails signaling content, the consistent-terminology matching the user's task vocabulary, the breadcrumb-trail state visibility) and the scent-killers (the mystery-icons, the jargon-mismatch labels, the dead-end navigations without return-scent) as the wayfinding-cue toolkit; the orientation-load management — the where-am-I problem: the documented disorientation costs (the lost-in-hyperspace findings from hypertext research transferred to dashboard hierarchies — the re-orientation working-memory tax after navigation: rebuilding the context model consumes the capacity saved by the destination), with the orientation-support patterns (the persistent-context anchors — the always-visible location indicators, the transition-continuity animations preserving the spatial mental model — the course-2 motion-continuity applied to navigation, the overview-map patterns — the miniaturized global view with position highlight); the navigation-structure load — the architecture effects: the breadth-depth trade-off quantified (the documented menu-structure findings: the optimal breadth at each level minimizing total navigation cost — the wide-shallow advantage for load with the scan-cost ceiling), the structure-consistency requirement (the same navigation grammar across the system — the automated wayfinding schema), and the path-frequency alignment (the common-task paths as one-interaction routes, the rare paths tolerating depth — the frequency-weighted architecture) as the structure-load engineering; the search-and-filter load — the query interface: the search-as-navigation pattern (the direct-access alternative to hierarchy traversal with the vocabulary-matching requirement — the synonym and typo tolerance as scent), the filter-state visibility (the active-filter display preventing the forgotten-filter confusion — the state-amnesia load), and the query-result-orientation (the result-to-source context links — the finding-within-context patterns) as the query-load management; the cross-tool navigation — the ecosystem wayfinding: the multi-dashboard and system navigation (the portal-hierarchy scent design, the cross-system deep-links preserving context, the tab-and-window management load — the browser-level orientation costs) with the destination-consistency standard (the same metric looking and behaving the same across tools — the cross-system schema coherence) as the ecosystem-load discipline; the interruption-and-resumption load — the return navigation: the documented resumption costs (the interruption-recovery research: re-finding position and state after diversion — the attention-residue effect), with the resumption-support patterns (the session-state persistence, the return-to-last-position defaults, the change-since-last-visit summaries — the streaming-course-13 connection, the bookmark and annotation personalization) as the interrupted-user load relief; the mobile-and-small-screen navigation — the constrained wayfinding: the viewport-limited orientation (the scroll-position-as-navigation problem, the collapsed-hierarchy patterns — the drawer and bottom-sheet navigation with the context-loss trade-offs, the thumb-reach interaction-cost geography) as the small-screen load adaptation; the foraging-measurement practice — the navigation evidence: the path-analysis telemetry (the actual navigation sequences versus designed paths — the revealed-foraging data, the dead-end and backtrack rates as the scent-failure metrics), the task-time decomposition (the navigation-fraction of total task time — the wayfinding-cost quantification), and the disorientation-event tracking (the rapid-back-navigation and search-after-navigation patterns as the lost-user signals) as the foraging-evidence system; the navigation anti-patterns — the failure catalog: the scent-free-iconography (the symbol-only navigation requiring learned-decoder rings — the expert-only wayfinding), the context-destroying navigation (the full-page transitions losing orientation — the teleportation failure), the filter-amnesia (invisible active filters corrupting comprehension — the hidden-state load), and the hierarchy-first-default (forcing navigation for the most common task — the zero-interaction-value violation from section 9) as the wayfinding-hygiene rules; and the foraging deliverable — the scent-rich, orientation-preserving, resumption-supporting navigation-load architecture. The module should assume the learner is progressing from competent beginner toward expert practitioner, and should function simultaneously as a learning resource, a practical reference, and a working methodology.

Convert each of the following capabilities the learner must gain into explicit “Teach how to” treatment with a practical framework the learner could use on a real project, not merely theory:

* Teach how to engineer information foraging through scent design (descriptive labels, previews, task-vocabulary matching, breadcrumb visibility) against cost-value foraging economics.

* Teach how to manage orientation load with persistent anchors, transition continuity, and overview maps countering lost-in-hyperspace re-orientation taxes.

* Teach how to optimize navigation structures via breadth-depth trade-offs, frequency-weighted paths, and consistent grammars, support search-filter wayfinding with state visibility and interruption resumption patterns.

* Teach how to adapt to mobile viewport constraints.

* Teach how to measure through path telemetry, task-time decomposition, and disorientation-event tracking preventing scent-free, context-destroying, filter-amnesia, and hierarchy-first failures.

Use numerous realistic examples throughout the module, drawn from professional practice in The science of working-memory limits applied to information design — managing intrinsic, extraneous, and germane load so complex dashboards inform users without overwhelming or fatiguing them. For each important principle, show weak, improved, and professional-level versions of the same work where useful.

Include practical exercises that require the learner to:

1. Engineer information foraging through scent design (descriptive labels, previews, task-vocabulary matching, breadcrumb visibility) against cost-value foraging economics, applied to a realistic scenario provided with the exercise.

2. Manage orientation load with persistent anchors, transition continuity, and overview maps countering lost-in-hyperspace re-orientation taxes, applied to a realistic scenario provided with the exercise.

3. Optimize navigation structures via breadth-depth trade-offs, frequency-weighted paths, and consistent grammars, support search-filter wayfinding with state visibility and interruption resumption patterns, applied to a realistic scenario provided with the exercise.

4. Adapt to mobile viewport constraints, applied to a realistic scenario provided with the exercise.

5. Measure through path telemetry, task-time decomposition, and disorientation-event tracking preventing scent-free, context-destroying, filter-amnesia, and hierarchy-first failures, applied to a realistic scenario provided with the exercise.

6. Integrate the full section: take one realistic problem in Cognitive load through every capability above as a coherent capstone deliverable.

For each exercise, provide the scenario, objective, constraints, expected deliverable, evaluation criteria, and an expert-quality example solution where appropriate.

Structure the material progressively: establish the fundamental concepts and distinctions of this section first, then move toward advanced professional application, showing how the concepts interact only after each is clear on its own.

Use professional terminology from The science of working-memory limits applied to information design — managing intrinsic, extraneous, and germane load so complex dashboards inform users without overwhelming or fatiguing them where relevant, but explain each specialized term in clear language on first use. Do not make the material sound like generic AI-generated advice; it should read as if written by an experienced practitioner.

Research the subject using high-quality professional and academic sources. Prioritize authoritative sources such as Sweller's cognitive load theory — the instructional-design foundation (intrinsic, extraneous, and germane load with the element-interactivity basis); Baddeley's working-memory model (phonological loop, visuospatial sketchpad, central executive) and Cowan's revised capacity findings (the four-chunk limit); Miller's magical number seven lineage with the chunking and recoding tradition; Mayer's multimedia learning principles (coherence, signaling, spatial-contiguity, temporal-contiguity, modality, redundancy) as the experimentally tested load-management toolkit; Paas-van Merrienboer load measurement tradition (subjective rating scales, task-performance triangulation) and NASA-TLX workload assessment; Stephen Few's dashboard design doctrine (glanceability, information density, chart-selection for monitoring) as the applied BI synthesis; Pirolli-Card information foraging theory for navigation-load and scent-following analysis. Avoid relying on low-quality SEO articles, content farms, or unsupported “best practice” claims; distinguish established research findings from professional conventions and informed recommendations, and never invent statistics or citations.

Where a recommendation depends on context, explicitly explain the trade-off rather than presenting it as an absolute rule.

End the module with actionable professional checklists — a engineer-information-foraging-through checklist, a manage-orientation-load-with checklist, a optimize-navigation-structures-via checklist, a adapt-to-mobile-viewport checklist, a measure-through-path-telemetry checklist — that a practitioner could actually use before real-world delivery.

The final result should be comprehensive enough to serve as an advanced professional training module, organized clearly enough that a learner can study it progressively and apply each concept in practical work.
